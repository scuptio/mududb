# Mudu API Rust Demo

This demo mirrors the C# demo flow:

1. create a table
2. clear existing rows
3. insert sample rows
4. query rows through `Mudu::query(...)`

Run it with:

```bash
cargo run -p mudu_api_rust_demo
```
