name: Build

on:
  push:
    branches: [ main, master ]
    paths-ignore:
      - '**/*.md'
      - 'doc/**'
      - '.github/run-actions-locally.md'
  pull_request:
    branches: [ main, master ]
    paths-ignore:
      - '**/*.md'
      - 'doc/**'
      - '.github/run-actions-locally.md'
  workflow_dispatch:
    inputs:
      clean_cargo:
        description: 'Run cargo clean before build'
        required: false
        type: boolean
        default: false
      release_test:
        description: 'Run tests in release mode'
        required: false
        type: boolean
        default: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  build:
    name: Build - ${{ matrix.os }} - ${{ matrix.group }}
    runs-on: ${{ matrix.os }}
    env:
      ENABLE_CARGO_CLEAN: ${{ github.event.inputs.clean_cargo || 'true' }}
      ENABLE_RELEASE_TEST: ${{ github.event.inputs.release_test || 'true' }}
      CARGO_INCREMENTAL: 0
      CARGO_BUILD_JOBS: 2
      CARGO_PROFILE_DEV_DEBUG: 0
      CARGO_PROFILE_TEST_DEBUG: 0
      CARGO_PROFILE_RELEASE_DEBUG: 0
      MUDU_TEST_BACKEND_READY_TIMEOUT_SECS: 120
      MUDU_CLI_HTTP_TIMEOUT_SECS: 120
    strategy:
      matrix:
        os: [ ubuntu-latest ]
        # The repository has no root workspace; each group under crates/ is an
        # independent cargo workspace with its own Cargo.lock.
        group: [ common, db-kernel, sdk, tools ]
        include:
          - os: ubuntu-latest
            rust_target: x86_64-unknown-linux-gnu
    defaults:
      run:
        shell: bash
        working-directory: crates/${{ matrix.group }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Setup pinned stable Rust
        if: matrix.os == 'ubuntu-latest'
        uses: ./.github/actions/rust-stable-setup

      - name: Free disk space
        if: matrix.os == 'ubuntu-latest'
        run: |
          df -h
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          docker system prune -af || true
          df -h

      - name: Cache Cargo Registry
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-stable-${{ matrix.group }}-${{ hashFiles('rust-toolchain.toml', '**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-stable-${{ matrix.group }}

      - name: Clean cargo cache (optional)
        if: env.ENABLE_CARGO_CLEAN == 'true'
        run: |
          mkdir -p ${{ github.workspace }}/logs
          set -o pipefail
          cargo clean 2>&1 | tee ${{ github.workspace }}/logs/build-clean-${{ matrix.group }}.log

      - name: Check (debug)
        if: env.ENABLE_RELEASE_TEST != 'true'
        run: |
          mkdir -p ${{ github.workspace }}/logs
          set -o pipefail
          cargo check 2>&1 | tee ${{ github.workspace }}/logs/build-check-debug-${{ matrix.group }}.log

      - name: Check (release)
        if: env.ENABLE_RELEASE_TEST == 'true'
        run: |
          mkdir -p ${{ github.workspace }}/logs
          set -o pipefail
          cargo check --release 2>&1 | tee ${{ github.workspace }}/logs/build-check-release-${{ matrix.group }}.log

      - name: Clippy
        run: |
          mkdir -p ${{ github.workspace }}/logs
          set -o pipefail
          cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tee ${{ github.workspace }}/logs/build-clippy-${{ matrix.group }}.log

      - name: Verify Install Script (Linux)
        if: matrix.os == 'ubuntu-latest' && matrix.group == 'tools'
        working-directory: ${{ github.workspace }}
        run: |
          mkdir -p logs
          set -o pipefail
          python3 script/build/install_binaries.py --dry-run 2>&1 | tee logs/build-verify-install.log

      - name: Inspect io_uring Environment (Linux)
        if: matrix.os == 'ubuntu-latest' && matrix.group == 'db-kernel'
        working-directory: ${{ github.workspace }}
        run: |
          mkdir -p logs
          set -o pipefail
          bash script/build/inspect_iouring_env.sh 2>&1 | tee logs/build-inspect-iouring.log

      - name: Test (debug)
        if: env.ENABLE_RELEASE_TEST != 'true'
        run: |
          mkdir -p ${{ github.workspace }}/logs
          set -o pipefail
          cargo test -- --test-threads=1 2>&1 | tee ${{ github.workspace }}/logs/build-test-debug-${{ matrix.group }}.log

      - name: Test (release)
        if: env.ENABLE_RELEASE_TEST == 'true'
        run: |
          mkdir -p ${{ github.workspace }}/logs
          set -o pipefail
          cargo test --release -- --test-threads=1 2>&1 | tee ${{ github.workspace }}/logs/build-test-release-${{ matrix.group }}.log

  rustdoc:
    name: Rustdoc
    runs-on: ubuntu-latest
    env:
      CARGO_INCREMENTAL: 0
    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Setup pinned stable Rust
        uses: ./.github/actions/rust-stable-setup

      - name: Cache Cargo Registry
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-doc-${{ hashFiles('rust-toolchain.toml', '**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-doc

      - name: Generate documentation
        run: |
          # No root workspace exists; document each group workspace in turn.
          for group in crates/common crates/db-kernel crates/sdk crates/tools; do
            echo "=== cargo doc: ${group} ==="
            (cd "${group}" && cargo doc --workspace --no-deps)
          done
