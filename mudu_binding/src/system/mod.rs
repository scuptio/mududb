//! Command and query invocation helpers for the system interface.

/// Command (DML) invocation serialization.
pub mod command_invoke;
/// Query (SELECT) invocation serialization.
pub mod query_invoke;
