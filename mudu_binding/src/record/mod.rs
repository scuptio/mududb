//! Record and field definitions that bridge universal record types with
//! `mudu` tuple descriptors.

/// Definition of a single record field (column).
pub mod field_def;
/// Definition of a record (table) composed of named fields.
pub mod record_def;
