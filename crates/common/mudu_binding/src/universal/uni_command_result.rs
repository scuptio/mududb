// Generated by mudu_gen. Do not edit manually.
#![allow(
    dead_code,
    unused_imports,
    clippy::borrowed_box,
    clippy::derivable_impls,
    clippy::map_identity,
    clippy::needless_borrow,
    clippy::panic,
    clippy::ptr_arg,
    clippy::redundant_closure,
    clippy::single_match,
    clippy::type_complexity
)]
use crate::universal::mp_wire::{
    FromValue, ToValue, Value, WireError, from_array, from_option, from_tuple2, from_tuple3,
    to_array, to_option, to_tuple2, to_tuple3,
};
use crate::universal::uni_error::UniError;
#[derive(Debug, Clone)]
pub enum UniCommandReturn {
    Ok(UniCommandResult),
    Err(UniError),
}
impl Default for UniCommandReturn {
    fn default() -> Self {
        Self::Ok(Default::default())
    }
}
impl UniCommandReturn {
    pub fn from_ok(inner: UniCommandResult) -> Self {
        Self::Ok(inner)
    }
    pub fn as_ok(&self) -> Option<&UniCommandResult> {
        match self {
            Self::Ok(inner) => Some(inner),
            _ => None,
        }
    }
    pub fn expect_ok(&self) -> &UniCommandResult {
        match self {
            Self::Ok(inner) => inner,
            _ => panic!("UniCommandReturn::expect_ok called on the wrong variant"),
        }
    }
    pub fn from_err(inner: UniError) -> Self {
        Self::Err(inner)
    }
    pub fn as_err(&self) -> Option<&UniError> {
        match self {
            Self::Err(inner) => Some(inner),
            _ => None,
        }
    }
    pub fn expect_err(&self) -> &UniError {
        match self {
            Self::Err(inner) => inner,
            _ => panic!("UniCommandReturn::expect_err called on the wrong variant"),
        }
    }
}
impl crate::universal::mp_wire::ToValue for UniCommandReturn {
    fn to_value(&self) -> crate::universal::mp_wire::Value {
        match self {
            UniCommandReturn::Ok(inner) => crate::universal::mp_wire::Value::Array(vec![
                crate::universal::mp_wire::Value::from(0u32),
                (inner).to_value(),
            ]),
            UniCommandReturn::Err(inner) => crate::universal::mp_wire::Value::Array(vec![
                crate::universal::mp_wire::Value::from(1u32),
                (inner).to_value(),
            ]),
        }
    }
}
impl crate::universal::mp_wire::FromValue for UniCommandReturn {
    fn from_value(
        value: &crate::universal::mp_wire::Value,
    ) -> Result<Self, crate::universal::mp_wire::WireError> {
        let items = value.as_array()?;
        if items.len() != 2 {
            return Err(crate::universal::mp_wire::WireError::new(format!(
                "a {} value must be a 2-element [tag, payload] array, found {} elements",
                "UniCommandReturn",
                items.len()
            )));
        }
        let tag = u32::from_value(&items[0])?;
        match tag {
            0 => Ok(Self::Ok(UniCommandResult::from_value(&items[1])?)),
            1 => Ok(Self::Err(UniError::from_value(&items[1])?)),
            _ => Err(crate::universal::mp_wire::WireError::new(format!(
                "unknown UniCommandReturn tag {tag}"
            ))),
        }
    }
}
#[derive(Debug, Clone)]
pub struct UniCommandResult {
    pub affected_rows: u64,
}
impl Default for UniCommandResult {
    fn default() -> Self {
        Self {
            affected_rows: Default::default(),
        }
    }
}
impl crate::universal::mp_wire::ToValue for UniCommandResult {
    fn to_value(&self) -> crate::universal::mp_wire::Value {
        crate::universal::mp_wire::Value::Map(vec![(
            crate::universal::mp_wire::Value::from(1u32),
            (&self.affected_rows).to_value(),
        )])
    }
}
impl crate::universal::mp_wire::FromValue for UniCommandResult {
    fn from_value(
        value: &crate::universal::mp_wire::Value,
    ) -> Result<Self, crate::universal::mp_wire::WireError> {
        let map_pairs = value.as_map()?;
        let mut affected_rows = None;
        for (key, val) in map_pairs {
            let Ok(number) = key.as_u64() else {
                continue;
            };
            match number {
                1 => affected_rows = Some(u64::from_value(val)?),
                _ => {}
            }
        }
        Ok(Self {
            affected_rows: affected_rows.unwrap_or_default(),
        })
    }
}
