// Generated by mudu_gen. Do not edit manually.
namespace mududb.types {

using MessagePack;
using MessagePack.Formatters;
using System.Collections.Generic;




[MessagePackFormatter(typeof(UniProcedureResultFormatter))]
public struct UniProcedureResult {
    
    [global::System.Diagnostics.CodeAnalysis.SetsRequiredMembers]
    public UniProcedureResult()
    {
        
        ReturnList = [];
        
    }
    
    
    
    public required List<UniDataValue> ReturnList { get; set; }
    
}

public class UniProcedureResultFormatter : IMessagePackFormatter<UniProcedureResult>
{
    public void Serialize(ref MessagePackWriter writer, UniProcedureResult value, MessagePackSerializerOptions options)
    {
        // The record encodes as a MessagePack map keyed by the 1-based field
        // numbers (WIT declaration order).
        writer.WriteMapHeader(1);
        
        writer.Write(1);
        if (value.ReturnList is null)
        {
            writer.WriteNil();
        }
        else
        {
            writer.WriteArrayHeader(value.ReturnList.Count);
            foreach (var __e0 in value.ReturnList)
            {
                new UniDataValueFormatter().Serialize(ref writer, __e0, options);
            }
        }

        
    }

    public UniProcedureResult Deserialize(ref MessagePackReader reader, MessagePackSerializerOptions options)
    {
        // Decode is lenient: integer keys of any MessagePack width are
        // accepted, non-integer and unknown keys are skipped, key order is
        // arbitrary, and missing fields keep the constructor defaults
        // (proto3 semantics).
        var count = reader.ReadMapHeader();
        var value = new UniProcedureResult();
        for (var __i = 0; __i < count; __i++)
        {
            if (reader.NextMessagePackType != MessagePackType.Integer)
            {
                reader.Skip();
                reader.Skip();
                continue;
            }

            switch (reader.ReadInt64())
            {
                
                case 1:
                {
        if (reader.TryReadNil())
        {
            value.ReturnList = null!;
        }
        else
        {
            var __c1 = reader.ReadArrayHeader();
            var __l1 = new List<UniDataValue>(__c1);
            for (var __i1 = 0; __i1 < __c1; __i1++)
            {
                __l1.Add(new UniDataValueFormatter().Deserialize(ref reader, options));
            }
            value.ReturnList = __l1;
        }

                    break;
                }
                
                default:
                    reader.Skip();
                    break;
            }
        }

        return value;
    }
}

}