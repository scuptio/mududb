// Generated by mudu_gen. Do not edit manually.

import { UniDataValue, UniDataValueCodec } from "./UniDataValue";

import { MpackWriter, MpackReader } from "../mpack";




export class UniProcedureResult {
    
    
    return_list: Array<UniDataValue> = new Array<UniDataValue>(0);
    
}

export class UniProcedureResultCodec {
    static encode(value: UniProcedureResult, writer: MpackWriter): void {
        writer.writeMapHeader(1);
        
        writer.writeU64(1);
        writer.writeArrayHeader(value.return_list.length);
        for (let i = 0; i < value.return_list.length; i++) {
            const item = value.return_list[i];
            UniDataValueCodec.encode(item, writer);
        }
        
    }

    static decode(reader: MpackReader): UniProcedureResult {
        const value = new UniProcedureResult();
        const count = reader.readMapHeader();
        for (let i: u32 = 0; i < count; i++) {
            const key = reader.readMapKey();
            
            
            if (key == 1) {
                {
                    const n = reader.readArrayHeader() as i32;
                    const arr = new Array<UniDataValue>(n);
                    for (let i = 0; i < n; i++) {
                        arr[i] = UniDataValueCodec.decode(reader);
                    }
                    value.return_list = arr;
                }
            } else {
                reader.skipValue();
            }
            
        }
        return value;
    }
}
