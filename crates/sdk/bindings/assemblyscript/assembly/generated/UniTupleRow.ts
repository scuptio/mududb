// Generated by mudu_gen. Do not edit manually.

import { UniDataValue, UniDataValueCodec } from "./UniDataValue";

import { MpackWriter, MpackReader } from "../mpack";




export class UniTupleRow {
    
    
    fields: Array<UniDataValue> = new Array<UniDataValue>(0);
    
}

export class UniTupleRowCodec {
    static encode(value: UniTupleRow, writer: MpackWriter): void {
        writer.writeMapHeader(1);
        
        writer.writeU64(1);
        writer.writeArrayHeader(value.fields.length);
        for (let i = 0; i < value.fields.length; i++) {
            const item = value.fields[i];
            UniDataValueCodec.encode(item, writer);
        }
        
    }

    static decode(reader: MpackReader): UniTupleRow {
        const value = new UniTupleRow();
        const count = reader.readMapHeader();
        for (let i: u32 = 0; i < count; i++) {
            const key = reader.readMapKey();
            
            
            if (key == 1) {
                {
                    const n = reader.readArrayHeader() as i32;
                    const arr = new Array<UniDataValue>(n);
                    for (let i = 0; i < n; i++) {
                        arr[i] = UniDataValueCodec.decode(reader);
                    }
                    value.fields = arr;
                }
            } else {
                reader.skipValue();
            }
            
        }
        return value;
    }
}
