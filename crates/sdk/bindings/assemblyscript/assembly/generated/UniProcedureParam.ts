// Generated by mudu_gen. Do not edit manually.

import { UniOid, UniOidCodec } from "./UniOid";

import { UniDataValue, UniDataValueCodec } from "./UniDataValue";

import { MpackWriter, MpackReader } from "../mpack";




export class UniProcedureParam {
    
    
    procedure: u64 = 0;
    
    
    session: UniOid = new UniOid();
    
    
    param_list: Array<UniDataValue> = new Array<UniDataValue>(0);
    
}

export class UniProcedureParamCodec {
    static encode(value: UniProcedureParam, writer: MpackWriter): void {
        writer.writeMapHeader(3);
        
        writer.writeU64(1);
        writer.writeU64(value.procedure);
        
        writer.writeU64(2);
        UniOidCodec.encode(value.session, writer);
        
        writer.writeU64(3);
        writer.writeArrayHeader(value.param_list.length);
        for (let i = 0; i < value.param_list.length; i++) {
            const item = value.param_list[i];
            UniDataValueCodec.encode(item, writer);
        }
        
    }

    static decode(reader: MpackReader): UniProcedureParam {
        const value = new UniProcedureParam();
        const count = reader.readMapHeader();
        for (let i: u32 = 0; i < count; i++) {
            const key = reader.readMapKey();
            
            
            if (key == 1) {
                value.procedure = reader.readU64();
            } 
            else if (key == 2) {
                value.session = UniOidCodec.decode(reader);
            } 
            else if (key == 3) {
                {
                    const n = reader.readArrayHeader() as i32;
                    const arr = new Array<UniDataValue>(n);
                    for (let i = 0; i < n; i++) {
                        arr[i] = UniDataValueCodec.decode(reader);
                    }
                    value.param_list = arr;
                }
            } else {
                reader.skipValue();
            }
            
        }
        return value;
    }
}
