// Generated by mudu_gen. Do not edit manually.
/*
 * mududb C binding message types/codecs (mgen message -l c
 * --with-func-codec). Self-contained header: `MP_INLINE` functions only
 * (`static inline` in C, `inline` in C++), no mutable state. Ownership
 * contract: encode borrows its inputs; decode copies every variable-length
 * value into the caller's `mp_arena`; the writer owns its buffer
 * (`mpw_free`). See the package readme.md.
 */
#ifndef MUDUD_TYPES_UNI_QUERY_RESULT_H
#define MUDUD_TYPES_UNI_QUERY_RESULT_H

#include <stdbool.h>
#include <stdint.h>
#include <string.h>

#include "mududb/codec/mpack.h"
#include "mududb/types/UniRecordType.h"
#include "mududb/types/UniResultSet.h"

#ifdef __cplusplus
extern "C" {
#endif

#ifndef MUDUD_FWD_UNI_QUERY_RETURN
#define MUDUD_FWD_UNI_QUERY_RETURN
typedef struct uni_query_return uni_query_return;
MP_INLINE void uni_query_return_encode(mp_writer *w, const uni_query_return *value);
MP_INLINE int uni_query_return_decode(mp_reader *r, mp_arena *a, uni_query_return *out);
MP_INLINE uni_query_return *uni_query_return_decode_new(mp_reader *r, mp_arena *a);
#endif /* MUDUD_FWD_UNI_QUERY_RETURN */
#ifndef MUDUD_FWD_UNI_QUERY_RESULT
#define MUDUD_FWD_UNI_QUERY_RESULT
typedef struct uni_query_result uni_query_result;
MP_INLINE void uni_query_result_encode(mp_writer *w, const uni_query_result *value);
MP_INLINE int uni_query_result_decode(mp_reader *r, mp_arena *a, uni_query_result *out);
MP_INLINE uni_query_result *uni_query_result_decode_new(mp_reader *r, mp_arena *a);
#endif /* MUDUD_FWD_UNI_QUERY_RESULT */
#ifndef MUDUD_FWD_UNI_ERROR
#define MUDUD_FWD_UNI_ERROR
typedef struct uni_error uni_error;
MP_INLINE void uni_error_encode(mp_writer *w, const uni_error *value);
MP_INLINE int uni_error_decode(mp_reader *r, mp_arena *a, uni_error *out);
MP_INLINE uni_error *uni_error_decode_new(mp_reader *r, mp_arena *a);
#endif /* MUDUD_FWD_UNI_ERROR */

typedef enum {
    UNI_QUERY_RETURN_OK = 0u,
    UNI_QUERY_RETURN_ERR = 1u,
} uni_query_return_kind;

struct uni_query_return {
    uni_query_return_kind kind;
    union {
        uni_query_result * ok;
        uni_error * err;
    } as;
};

struct uni_query_result {
    uni_record_type tuple_desc;
    uni_result_set result_set;
};

/* Encode `value` as a `[tag, payload]` 2-array; payload-less cases carry a
 * `0u8` placeholder (NOT nil), matching the Rust host's variant serde. An
 * unknown kind flags `w` (check `mpw_ok`) instead of emitting a corrupt
 * value. */
MP_INLINE void uni_query_return_encode(mp_writer *w, const uni_query_return *value) {
    mpw_array_header(w, 2u);
    mpw_u64(w, (uint64_t)value->kind);
    switch (value->kind) {
    case UNI_QUERY_RETURN_OK:
        if ((value->as.ok) == 0) {
            mpw_fail(w);
        } else {
            uni_query_result_encode(w, &((*value->as.ok)));
        }
        break;
    case UNI_QUERY_RETURN_ERR:
        if ((value->as.err) == 0) {
            mpw_fail(w);
        } else {
            uni_error_encode(w, &((*value->as.err)));
        }
        break;
    default:
        /* unknown kind: flag the writer instead of emitting a corrupt value */
        mpw_fail(w);
        break;
    }
}

/* Decode one variant into `value` (arena contract of the generated
 * decoders). 0 on success, -1 (sticky error set) on a malformed shape or an
 * unknown tag. */
MP_INLINE int uni_query_return_decode(mp_reader *r, mp_arena *a, uni_query_return *value) {
    (void)a;
    memset(value, 0, sizeof(*value));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        switch (tag) {
        case 0u:
            value->kind = UNI_QUERY_RETURN_OK;
            {
                uni_query_result *p0 = uni_query_result_decode_new(r, a);
                if (p0 == 0) {
                    mpr_fail(r);
                } else {
                    value->as.ok = p0;
                }
            }
            break;
        case 1u:
            value->kind = UNI_QUERY_RETURN_ERR;
            {
                uni_error *p0 = uni_error_decode_new(r, a);
                if (p0 == 0) {
                    mpr_fail(r);
                } else {
                    value->as.err = p0;
                }
            }
            break;
        default:
            mpr_fail(r);
            return -1;
        }
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Decode one variant into freshly arena-allocated storage; 0 on failure
 * (the reader's sticky error is set). Boxes of this type decode through
 * this function, so headers referencing it by pointer never need its
 * `sizeof`. */
MP_INLINE uni_query_return *uni_query_return_decode_new(mp_reader *r, mp_arena *a) {
    uni_query_return *p = (uni_query_return *)mpa_alloc(a, sizeof(uni_query_return));
    if (p == 0) {
        mpr_fail(r);
        return 0;
    }
    if (uni_query_return_decode(r, a, p) != 0) {
        return 0;
    }
    return p;
}

/* Encode `value` as a MessagePack map keyed by the 1-based field numbers
 * (WIT declaration order); `option` fields are omitted when null (proto3
 * presence semantics). Encode only borrows `value`; failures are sticky in
 * `w` (check `mpw_ok`). */
MP_INLINE void uni_query_result_encode(mp_writer *w, const uni_query_result *value) {
    mpw_map_header(w, 2u);
    mpw_u64(w, 1u);
    uni_record_type_encode(w, &(value->tuple_desc));
    mpw_u64(w, 2u);
    uni_result_set_encode(w, &(value->result_set));
}

/* Decode one record into `out`, borrowing nothing from the input: every
 * string, blob, array and boxed value is copied into `a`. Lenient: unknown
 * keys are skipped and missing fields keep their zero default (absent
 * `option` fields decode as null). 0 on success, -1 (with the reader's
 * sticky error set) on malformed input or arena exhaustion. */
MP_INLINE int uni_query_result_decode(mp_reader *r, mp_arena *a, uni_query_result *out) {
    (void)a;
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_record_type_decode(r, a, &(out->tuple_desc));
                } else if (key == 2u) {
                    uni_result_set_decode(r, a, &(out->result_set));
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Decode one record into freshly arena-allocated storage; 0 on failure
 * (the reader's sticky error is set). Boxes of this type decode through
 * this function, so headers referencing it by pointer never need its
 * `sizeof`. */
MP_INLINE uni_query_result *uni_query_result_decode_new(mp_reader *r, mp_arena *a) {
    uni_query_result *p = (uni_query_result *)mpa_alloc(a, sizeof(uni_query_result));
    if (p == 0) {
        mpr_fail(r);
        return 0;
    }
    if (uni_query_result_decode(r, a, p) != 0) {
        return 0;
    }
    return p;
}

#ifdef __cplusplus
}
#endif
#include "mududb/types/UniError.h"

#endif /* MUDUD_TYPES_UNI_QUERY_RESULT_H */