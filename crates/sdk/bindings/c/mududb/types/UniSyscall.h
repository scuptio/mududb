// Generated by mudu_gen. Do not edit manually.
/*
 * mududb C binding message types/codecs (mgen message -l c
 * --with-func-codec). Self-contained header: `MP_INLINE` functions only
 * (`static inline` in C, `inline` in C++), no mutable state. Ownership
 * contract: encode borrows its inputs; decode copies every variable-length
 * value into the caller's `mp_arena`; the writer owns its buffer
 * (`mpw_free`). See the package readme.md.
 */
#ifndef MUDUD_TYPES_UNI_SYSCALL_H
#define MUDUD_TYPES_UNI_SYSCALL_H

#include <stdbool.h>
#include <stdint.h>
#include <string.h>

#include "mududb/codec/mpack.h"
#include "mududb/types/UniCommandArgv.h"
#include "mududb/types/UniCommandResult.h"
#include "mududb/types/UniQueryArgv.h"
#include "mududb/types/UniQueryResult.h"
#include "mududb/types/UniError.h"
#include "mududb/types/UniOid.h"
#include "mududb/types/UniFsOpenArgv.h"
#include "mududb/types/UniFsStat.h"
#include "mududb/types/UniFsDirent.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Length in bytes of the fixed MSSP payload header. */
#define UNI_SYSCALL_MSSP_HEADER_LEN 16u

/* Syscall message kinds carried in the header's `message_kind` field. The
 * discriminants identify the WIT functions of this file and are pinned to
 * their 1-based declaration order; `0` and unknown values are rejected by
 * `uni_syscall_decode_header`. */
typedef enum {
    UNI_SYSCALL_QUERY = 1u,
    UNI_SYSCALL_COMMAND = 2u,
    UNI_SYSCALL_BATCH = 3u,
    UNI_SYSCALL_OPEN_SESSION = 4u,
    UNI_SYSCALL_CLOSE_SESSION = 5u,
    UNI_SYSCALL_GET = 6u,
    UNI_SYSCALL_PUT = 7u,
    UNI_SYSCALL_DELETE = 8u,
    UNI_SYSCALL_RANGE = 9u,
    UNI_SYSCALL_FS_OPEN = 10u,
    UNI_SYSCALL_FS_CLOSE = 11u,
    UNI_SYSCALL_FS_READ = 12u,
    UNI_SYSCALL_FS_WRITE = 13u,
    UNI_SYSCALL_FS_PREAD = 14u,
    UNI_SYSCALL_FS_PWRITE = 15u,
    UNI_SYSCALL_FS_LSEEK = 16u,
    UNI_SYSCALL_FS_FSTAT = 17u,
    UNI_SYSCALL_FS_STAT = 18u,
    UNI_SYSCALL_FS_FSYNC = 19u,
    UNI_SYSCALL_FS_READDIR = 20u,
    UNI_SYSCALL_RELATION_GET = 21u,
    UNI_SYSCALL_RELATION_UPDATE = 22u,
    UNI_SYSCALL_RELATION_INSERT = 23u,
} uni_syscall_message_kind;

/* Convert a raw header value into a message kind; -1 on `0` and unknown
 * values. */
MP_INLINE int uni_syscall_message_kind_from_u32(uint32_t raw, uni_syscall_message_kind *out) {
    switch (raw) {
    case 1u:
        *out = UNI_SYSCALL_QUERY;
        return 0;
    case 2u:
        *out = UNI_SYSCALL_COMMAND;
        return 0;
    case 3u:
        *out = UNI_SYSCALL_BATCH;
        return 0;
    case 4u:
        *out = UNI_SYSCALL_OPEN_SESSION;
        return 0;
    case 5u:
        *out = UNI_SYSCALL_CLOSE_SESSION;
        return 0;
    case 6u:
        *out = UNI_SYSCALL_GET;
        return 0;
    case 7u:
        *out = UNI_SYSCALL_PUT;
        return 0;
    case 8u:
        *out = UNI_SYSCALL_DELETE;
        return 0;
    case 9u:
        *out = UNI_SYSCALL_RANGE;
        return 0;
    case 10u:
        *out = UNI_SYSCALL_FS_OPEN;
        return 0;
    case 11u:
        *out = UNI_SYSCALL_FS_CLOSE;
        return 0;
    case 12u:
        *out = UNI_SYSCALL_FS_READ;
        return 0;
    case 13u:
        *out = UNI_SYSCALL_FS_WRITE;
        return 0;
    case 14u:
        *out = UNI_SYSCALL_FS_PREAD;
        return 0;
    case 15u:
        *out = UNI_SYSCALL_FS_PWRITE;
        return 0;
    case 16u:
        *out = UNI_SYSCALL_FS_LSEEK;
        return 0;
    case 17u:
        *out = UNI_SYSCALL_FS_FSTAT;
        return 0;
    case 18u:
        *out = UNI_SYSCALL_FS_STAT;
        return 0;
    case 19u:
        *out = UNI_SYSCALL_FS_FSYNC;
        return 0;
    case 20u:
        *out = UNI_SYSCALL_FS_READDIR;
        return 0;
    case 21u:
        *out = UNI_SYSCALL_RELATION_GET;
        return 0;
    case 22u:
        *out = UNI_SYSCALL_RELATION_UPDATE;
        return 0;
    case 23u:
        *out = UNI_SYSCALL_RELATION_INSERT;
        return 0;
    default:
        return -1;
    }
}

/* Append the fixed 16-byte header for `kind` to `w` (magic "MSSP",
 * version 1, zero flags; all header fields big-endian). */
MP_INLINE void uni_syscall_write_header(mp_writer *w, uni_syscall_message_kind kind) {
    uint8_t header[UNI_SYSCALL_MSSP_HEADER_LEN];
    uint32_t raw = (uint32_t)kind;
    memset(header, 0, sizeof(header));
    header[0] = 0x4du; /* 'M' */
    header[1] = 0x53u; /* 'S' */
    header[2] = 0x53u; /* 'S' */
    header[3] = 0x50u; /* 'P' */
    header[7] = 1u;    /* version 1 (big-endian u32) */
    header[12] = (uint8_t)(raw >> 24);
    header[13] = (uint8_t)(raw >> 16);
    header[14] = (uint8_t)(raw >> 8);
    header[15] = (uint8_t)raw;
    mpw_raw(w, header, sizeof(header));
}

/* Read and validate the fixed 16-byte header, returning the message kind in
 * `out`; -1 (with the reader's sticky error set) on any integrity
 * violation. */
MP_INLINE int uni_syscall_decode_header(mp_reader *r, uni_syscall_message_kind *out) {
    uint8_t header[UNI_SYSCALL_MSSP_HEADER_LEN];
    uint32_t magic;
    uint32_t version;
    uint32_t flags;
    uint32_t raw;
    if (mpr_raw(r, header, sizeof(header)) != 0) {
        return -1;
    }
    magic = ((uint32_t)header[0] << 24) | ((uint32_t)header[1] << 16)
        | ((uint32_t)header[2] << 8) | (uint32_t)header[3];
    if (magic != 0x4d535350u) {
        mpr_fail(r);
        return -1;
    }
    version = ((uint32_t)header[4] << 24) | ((uint32_t)header[5] << 16)
        | ((uint32_t)header[6] << 8) | (uint32_t)header[7];
    if (version != 1u) {
        mpr_fail(r);
        return -1;
    }
    flags = ((uint32_t)header[8] << 24) | ((uint32_t)header[9] << 16)
        | ((uint32_t)header[10] << 8) | (uint32_t)header[11];
    if (flags != 0u) {
        mpr_fail(r);
        return -1;
    }
    raw = ((uint32_t)header[12] << 24) | ((uint32_t)header[13] << 16)
        | ((uint32_t)header[14] << 8) | (uint32_t)header[15];
    if (uni_syscall_message_kind_from_u32(raw, out) != 0) {
        mpr_fail(r);
        return -1;
    }
    return 0;
}

/* Encode one `query` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_query_request_encode(mp_writer *w, const uni_query_argv *argv) {
    uni_syscall_write_header(w, UNI_SYSCALL_QUERY);
    mpw_map_header(w, 1u);
    mpw_u64(w, 1u);
    uni_query_argv_encode(w, &((*argv)));
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `query` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_query_request_decode(mp_reader *r, mp_arena *a, uni_query_argv *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_QUERY) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_query_argv_decode(r, a, &((*out)));
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `query`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uni_query_result value;
} uni_syscall_query_result;

/* Encode one `query` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_query_result_encode(mp_writer *w, const uni_syscall_query_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_QUERY);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        uni_query_result_encode(w, &(res->value));
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `query` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_query_result_decode(mp_reader *r, mp_arena *a, uni_syscall_query_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_QUERY) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            uni_query_result_decode(r, a, &(out->value));
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Encode one `command` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_command_request_encode(mp_writer *w, const uni_command_argv *argv) {
    uni_syscall_write_header(w, UNI_SYSCALL_COMMAND);
    mpw_map_header(w, 1u);
    mpw_u64(w, 1u);
    uni_command_argv_encode(w, &((*argv)));
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `command` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_command_request_decode(mp_reader *r, mp_arena *a, uni_command_argv *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_COMMAND) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_command_argv_decode(r, a, &((*out)));
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `command`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uni_command_result value;
} uni_syscall_command_result;

/* Encode one `command` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_command_result_encode(mp_writer *w, const uni_syscall_command_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_COMMAND);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        uni_command_result_encode(w, &(res->value));
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `command` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_command_result_decode(mp_reader *r, mp_arena *a, uni_syscall_command_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_COMMAND) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            uni_command_result_decode(r, a, &(out->value));
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Encode one `batch` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_batch_request_encode(mp_writer *w, const uni_command_argv *argv) {
    uni_syscall_write_header(w, UNI_SYSCALL_BATCH);
    mpw_map_header(w, 1u);
    mpw_u64(w, 1u);
    uni_command_argv_encode(w, &((*argv)));
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `batch` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_batch_request_decode(mp_reader *r, mp_arena *a, uni_command_argv *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_BATCH) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_command_argv_decode(r, a, &((*out)));
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `batch`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uni_command_result value;
} uni_syscall_batch_result;

/* Encode one `batch` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_batch_result_encode(mp_writer *w, const uni_syscall_batch_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_BATCH);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        uni_command_result_encode(w, &(res->value));
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `batch` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_batch_result_decode(mp_reader *r, mp_arena *a, uni_syscall_batch_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_BATCH) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            uni_command_result_decode(r, a, &(out->value));
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Encode one `open_session` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_open_session_request_encode(mp_writer *w, const uni_oid *worker_id) {
    uni_syscall_write_header(w, UNI_SYSCALL_OPEN_SESSION);
    mpw_map_header(w, 1u);
    mpw_u64(w, 1u);
    uni_oid_encode(w, &((*worker_id)));
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `open_session` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_open_session_request_decode(mp_reader *r, mp_arena *a, uni_oid *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_OPEN_SESSION) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_oid_decode(r, a, &((*out)));
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `open_session`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uni_oid value;
} uni_syscall_open_session_result;

/* Encode one `open_session` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_open_session_result_encode(mp_writer *w, const uni_syscall_open_session_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_OPEN_SESSION);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        uni_oid_encode(w, &(res->value));
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `open_session` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_open_session_result_decode(mp_reader *r, mp_arena *a, uni_syscall_open_session_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_OPEN_SESSION) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            uni_oid_decode(r, a, &(out->value));
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Encode one `close_session` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_close_session_request_encode(mp_writer *w, const uni_oid *oid) {
    uni_syscall_write_header(w, UNI_SYSCALL_CLOSE_SESSION);
    mpw_map_header(w, 1u);
    mpw_u64(w, 1u);
    uni_oid_encode(w, &((*oid)));
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `close_session` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_close_session_request_decode(mp_reader *r, mp_arena *a, uni_oid *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_CLOSE_SESSION) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_oid_decode(r, a, &((*out)));
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `close_session`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
} uni_syscall_close_session_result;

/* Encode one `close_session` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_close_session_result_encode(mp_writer *w, const uni_syscall_close_session_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_CLOSE_SESSION);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_u64(w, 0u);
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `close_session` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_close_session_result_decode(mp_reader *r, mp_arena *a, uni_syscall_close_session_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_CLOSE_SESSION) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            (void)mpr_u64(r);
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct { int has_value; mp_bin value; } uni_syscall_get_result_opt;
typedef struct {
    uni_oid oid;
    mp_bin key;
} uni_syscall_get_request;

/* Encode one `get` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_get_request_encode(mp_writer *w, const uni_oid *oid, mp_bin key) {
    uni_syscall_write_header(w, UNI_SYSCALL_GET);
    mpw_map_header(w, 2u);
    mpw_u64(w, 1u);
    uni_oid_encode(w, &((*oid)));
    mpw_u64(w, 2u);
    mpw_bin(w, (key).data, (key).len);
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `get` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_get_request_decode(mp_reader *r, mp_arena *a, uni_syscall_get_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_GET) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_oid_decode(r, a, &(out->oid));
                } else if (key == 2u) {
                    {
                        uint32_t n0 = 0;
                        const uint8_t *s0 = mpr_bin(r, &n0);
                        uint8_t *p0 = (uint8_t *)mpa_alloc(a, (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            memcpy(p0, s0, n0);
                            (out->key).data = p0;
                            (out->key).len = n0;
                        }
                    }
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `get`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uni_syscall_get_result_opt value;
} uni_syscall_get_result;

/* Encode one `get` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_get_result_encode(mp_writer *w, const uni_syscall_get_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_GET);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        if ((res->value).has_value) {
            mpw_bin(w, ((res->value).value).data, ((res->value).value).len);
        } else {
            mpw_nil(w);
        }
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `get` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_get_result_decode(mp_reader *r, mp_arena *a, uni_syscall_get_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_GET) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            memset(&(out->value), 0, sizeof(out->value));
            if (mpr_try_nil(r)) {
                (out->value).has_value = 0;
            } else {
                (out->value).has_value = 1;
                {
                    uint32_t n1 = 0;
                    const uint8_t *s1 = mpr_bin(r, &n1);
                    uint8_t *p1 = (uint8_t *)mpa_alloc(a, (size_t)n1);
                    if (p1 == 0) {
                        mpr_fail(r);
                    } else {
                        memcpy(p1, s1, n1);
                        ((out->value).value).data = p1;
                        ((out->value).value).len = n1;
                    }
                }
            }
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct {
    uni_oid oid;
    mp_bin key;
    mp_bin value;
} uni_syscall_put_request;

/* Encode one `put` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_put_request_encode(mp_writer *w, const uni_oid *oid, mp_bin key, mp_bin value) {
    uni_syscall_write_header(w, UNI_SYSCALL_PUT);
    mpw_map_header(w, 3u);
    mpw_u64(w, 1u);
    uni_oid_encode(w, &((*oid)));
    mpw_u64(w, 2u);
    mpw_bin(w, (key).data, (key).len);
    mpw_u64(w, 3u);
    mpw_bin(w, (value).data, (value).len);
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `put` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_put_request_decode(mp_reader *r, mp_arena *a, uni_syscall_put_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_PUT) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_oid_decode(r, a, &(out->oid));
                } else if (key == 2u) {
                    {
                        uint32_t n0 = 0;
                        const uint8_t *s0 = mpr_bin(r, &n0);
                        uint8_t *p0 = (uint8_t *)mpa_alloc(a, (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            memcpy(p0, s0, n0);
                            (out->key).data = p0;
                            (out->key).len = n0;
                        }
                    }
                } else if (key == 3u) {
                    {
                        uint32_t n0 = 0;
                        const uint8_t *s0 = mpr_bin(r, &n0);
                        uint8_t *p0 = (uint8_t *)mpa_alloc(a, (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            memcpy(p0, s0, n0);
                            (out->value).data = p0;
                            (out->value).len = n0;
                        }
                    }
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `put`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
} uni_syscall_put_result;

/* Encode one `put` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_put_result_encode(mp_writer *w, const uni_syscall_put_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_PUT);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_u64(w, 0u);
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `put` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_put_result_decode(mp_reader *r, mp_arena *a, uni_syscall_put_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_PUT) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            (void)mpr_u64(r);
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct {
    uni_oid oid;
    mp_bin key;
} uni_syscall_delete_request;

/* Encode one `delete` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_delete_request_encode(mp_writer *w, const uni_oid *oid, mp_bin key) {
    uni_syscall_write_header(w, UNI_SYSCALL_DELETE);
    mpw_map_header(w, 2u);
    mpw_u64(w, 1u);
    uni_oid_encode(w, &((*oid)));
    mpw_u64(w, 2u);
    mpw_bin(w, (key).data, (key).len);
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `delete` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_delete_request_decode(mp_reader *r, mp_arena *a, uni_syscall_delete_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_DELETE) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_oid_decode(r, a, &(out->oid));
                } else if (key == 2u) {
                    {
                        uint32_t n0 = 0;
                        const uint8_t *s0 = mpr_bin(r, &n0);
                        uint8_t *p0 = (uint8_t *)mpa_alloc(a, (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            memcpy(p0, s0, n0);
                            (out->key).data = p0;
                            (out->key).len = n0;
                        }
                    }
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `delete`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
} uni_syscall_delete_result;

/* Encode one `delete` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_delete_result_encode(mp_writer *w, const uni_syscall_delete_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_DELETE);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_u64(w, 0u);
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `delete` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_delete_result_decode(mp_reader *r, mp_arena *a, uni_syscall_delete_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_DELETE) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            (void)mpr_u64(r);
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct {
    mp_bin f0;
    mp_bin f1;
} uni_syscall_range_result_tuple;
typedef struct { uni_syscall_range_result_tuple *items; uint32_t len; } uni_syscall_range_result_list;
typedef struct {
    uni_oid oid;
    mp_bin start;
    mp_bin end;
} uni_syscall_range_request;

/* Encode one `range` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_range_request_encode(mp_writer *w, const uni_oid *oid, mp_bin start, mp_bin end) {
    uni_syscall_write_header(w, UNI_SYSCALL_RANGE);
    mpw_map_header(w, 3u);
    mpw_u64(w, 1u);
    uni_oid_encode(w, &((*oid)));
    mpw_u64(w, 2u);
    mpw_bin(w, (start).data, (start).len);
    mpw_u64(w, 3u);
    mpw_bin(w, (end).data, (end).len);
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `range` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_range_request_decode(mp_reader *r, mp_arena *a, uni_syscall_range_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_RANGE) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_oid_decode(r, a, &(out->oid));
                } else if (key == 2u) {
                    {
                        uint32_t n0 = 0;
                        const uint8_t *s0 = mpr_bin(r, &n0);
                        uint8_t *p0 = (uint8_t *)mpa_alloc(a, (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            memcpy(p0, s0, n0);
                            (out->start).data = p0;
                            (out->start).len = n0;
                        }
                    }
                } else if (key == 3u) {
                    {
                        uint32_t n0 = 0;
                        const uint8_t *s0 = mpr_bin(r, &n0);
                        uint8_t *p0 = (uint8_t *)mpa_alloc(a, (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            memcpy(p0, s0, n0);
                            (out->end).data = p0;
                            (out->end).len = n0;
                        }
                    }
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `range`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uni_syscall_range_result_list value;
} uni_syscall_range_result;

/* Encode one `range` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_range_result_encode(mp_writer *w, const uni_syscall_range_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_RANGE);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_array_header(w, (res->value).len);
        for (uint32_t i0 = 0; i0 < (res->value).len; i0++) {
            mpw_array_header(w, 2u);
            mpw_bin(w, (((res->value).items[i0]).f0).data, (((res->value).items[i0]).f0).len);
            mpw_bin(w, (((res->value).items[i0]).f1).data, (((res->value).items[i0]).f1).len);
        }
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `range` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_range_result_decode(mp_reader *r, mp_arena *a, uni_syscall_range_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_RANGE) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            {
                uint32_t n0 = mpr_array_header(r);
                uni_syscall_range_result_tuple *p0 = (uni_syscall_range_result_tuple *)mpa_alloc(a, sizeof(uni_syscall_range_result_tuple) * (size_t)n0);
                if (p0 == 0) {
                    mpr_fail(r);
                } else {
                    for (uint32_t i0 = 0; i0 < n0; i0++) {
                        memset(&(p0[i0]), 0, sizeof(p0[i0]));
                        if (mpr_array_header(r) != 2u) {
                            mpr_fail(r);
                        } else {
                            {
                                uint32_t n2 = 0;
                                const uint8_t *s2 = mpr_bin(r, &n2);
                                uint8_t *p2 = (uint8_t *)mpa_alloc(a, (size_t)n2);
                                if (p2 == 0) {
                                    mpr_fail(r);
                                } else {
                                    memcpy(p2, s2, n2);
                                    ((p0[i0]).f0).data = p2;
                                    ((p0[i0]).f0).len = n2;
                                }
                            }
                            {
                                uint32_t n2 = 0;
                                const uint8_t *s2 = mpr_bin(r, &n2);
                                uint8_t *p2 = (uint8_t *)mpa_alloc(a, (size_t)n2);
                                if (p2 == 0) {
                                    mpr_fail(r);
                                } else {
                                    memcpy(p2, s2, n2);
                                    ((p0[i0]).f1).data = p2;
                                    ((p0[i0]).f1).len = n2;
                                }
                            }
                        }
                    }
                    (out->value).items = p0;
                    (out->value).len = n0;
                }
            }
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Encode one `fs_open` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_fs_open_request_encode(mp_writer *w, const uni_fs_open_argv *argv) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_OPEN);
    mpw_map_header(w, 1u);
    mpw_u64(w, 1u);
    uni_fs_open_argv_encode(w, &((*argv)));
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_open` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_fs_open_request_decode(mp_reader *r, mp_arena *a, uni_fs_open_argv *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_OPEN) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_fs_open_argv_decode(r, a, &((*out)));
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `fs_open`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uint32_t value;
} uni_syscall_fs_open_result;

/* Encode one `fs_open` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_fs_open_result_encode(mp_writer *w, const uni_syscall_fs_open_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_OPEN);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_u64(w, (uint64_t)(res->value));
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_open` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_fs_open_result_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_open_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_OPEN) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            out->value = (uint32_t)mpr_u64(r);
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Encode one `fs_close` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_fs_close_request_encode(mp_writer *w, uint32_t fd) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_CLOSE);
    mpw_map_header(w, 1u);
    mpw_u64(w, 1u);
    mpw_u64(w, (uint64_t)(fd));
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_close` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_fs_close_request_decode(mp_reader *r, mp_arena *a, uint32_t *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_CLOSE) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    (*out) = (uint32_t)mpr_u64(r);
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `fs_close`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
} uni_syscall_fs_close_result;

/* Encode one `fs_close` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_fs_close_result_encode(mp_writer *w, const uni_syscall_fs_close_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_CLOSE);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_u64(w, 0u);
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_close` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_fs_close_result_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_close_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_CLOSE) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            (void)mpr_u64(r);
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct {
    uint32_t fd;
    uint32_t len;
} uni_syscall_fs_read_request;

/* Encode one `fs_read` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_fs_read_request_encode(mp_writer *w, uint32_t fd, uint32_t len) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_READ);
    mpw_map_header(w, 2u);
    mpw_u64(w, 1u);
    mpw_u64(w, (uint64_t)(fd));
    mpw_u64(w, 2u);
    mpw_u64(w, (uint64_t)(len));
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_read` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_fs_read_request_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_read_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_READ) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    out->fd = (uint32_t)mpr_u64(r);
                } else if (key == 2u) {
                    out->len = (uint32_t)mpr_u64(r);
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `fs_read`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    mp_bin value;
} uni_syscall_fs_read_result;

/* Encode one `fs_read` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_fs_read_result_encode(mp_writer *w, const uni_syscall_fs_read_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_READ);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_bin(w, (res->value).data, (res->value).len);
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_read` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_fs_read_result_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_read_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_READ) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            {
                uint32_t n0 = 0;
                const uint8_t *s0 = mpr_bin(r, &n0);
                uint8_t *p0 = (uint8_t *)mpa_alloc(a, (size_t)n0);
                if (p0 == 0) {
                    mpr_fail(r);
                } else {
                    memcpy(p0, s0, n0);
                    (out->value).data = p0;
                    (out->value).len = n0;
                }
            }
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct {
    uint32_t fd;
    mp_bin data;
} uni_syscall_fs_write_request;

/* Encode one `fs_write` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_fs_write_request_encode(mp_writer *w, uint32_t fd, mp_bin data) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_WRITE);
    mpw_map_header(w, 2u);
    mpw_u64(w, 1u);
    mpw_u64(w, (uint64_t)(fd));
    mpw_u64(w, 2u);
    mpw_bin(w, (data).data, (data).len);
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_write` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_fs_write_request_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_write_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_WRITE) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    out->fd = (uint32_t)mpr_u64(r);
                } else if (key == 2u) {
                    {
                        uint32_t n0 = 0;
                        const uint8_t *s0 = mpr_bin(r, &n0);
                        uint8_t *p0 = (uint8_t *)mpa_alloc(a, (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            memcpy(p0, s0, n0);
                            (out->data).data = p0;
                            (out->data).len = n0;
                        }
                    }
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `fs_write`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uint32_t value;
} uni_syscall_fs_write_result;

/* Encode one `fs_write` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_fs_write_result_encode(mp_writer *w, const uni_syscall_fs_write_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_WRITE);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_u64(w, (uint64_t)(res->value));
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_write` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_fs_write_result_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_write_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_WRITE) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            out->value = (uint32_t)mpr_u64(r);
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct {
    uint32_t fd;
    uint64_t offset;
    uint32_t len;
} uni_syscall_fs_pread_request;

/* Encode one `fs_pread` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_fs_pread_request_encode(mp_writer *w, uint32_t fd, uint64_t offset, uint32_t len) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_PREAD);
    mpw_map_header(w, 3u);
    mpw_u64(w, 1u);
    mpw_u64(w, (uint64_t)(fd));
    mpw_u64(w, 2u);
    mpw_u64(w, (uint64_t)(offset));
    mpw_u64(w, 3u);
    mpw_u64(w, (uint64_t)(len));
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_pread` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_fs_pread_request_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_pread_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_PREAD) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    out->fd = (uint32_t)mpr_u64(r);
                } else if (key == 2u) {
                    out->offset = mpr_u64(r);
                } else if (key == 3u) {
                    out->len = (uint32_t)mpr_u64(r);
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `fs_pread`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    mp_bin value;
} uni_syscall_fs_pread_result;

/* Encode one `fs_pread` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_fs_pread_result_encode(mp_writer *w, const uni_syscall_fs_pread_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_PREAD);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_bin(w, (res->value).data, (res->value).len);
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_pread` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_fs_pread_result_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_pread_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_PREAD) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            {
                uint32_t n0 = 0;
                const uint8_t *s0 = mpr_bin(r, &n0);
                uint8_t *p0 = (uint8_t *)mpa_alloc(a, (size_t)n0);
                if (p0 == 0) {
                    mpr_fail(r);
                } else {
                    memcpy(p0, s0, n0);
                    (out->value).data = p0;
                    (out->value).len = n0;
                }
            }
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct {
    uint32_t fd;
    uint64_t offset;
    mp_bin data;
} uni_syscall_fs_pwrite_request;

/* Encode one `fs_pwrite` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_fs_pwrite_request_encode(mp_writer *w, uint32_t fd, uint64_t offset, mp_bin data) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_PWRITE);
    mpw_map_header(w, 3u);
    mpw_u64(w, 1u);
    mpw_u64(w, (uint64_t)(fd));
    mpw_u64(w, 2u);
    mpw_u64(w, (uint64_t)(offset));
    mpw_u64(w, 3u);
    mpw_bin(w, (data).data, (data).len);
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_pwrite` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_fs_pwrite_request_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_pwrite_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_PWRITE) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    out->fd = (uint32_t)mpr_u64(r);
                } else if (key == 2u) {
                    out->offset = mpr_u64(r);
                } else if (key == 3u) {
                    {
                        uint32_t n0 = 0;
                        const uint8_t *s0 = mpr_bin(r, &n0);
                        uint8_t *p0 = (uint8_t *)mpa_alloc(a, (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            memcpy(p0, s0, n0);
                            (out->data).data = p0;
                            (out->data).len = n0;
                        }
                    }
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `fs_pwrite`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
} uni_syscall_fs_pwrite_result;

/* Encode one `fs_pwrite` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_fs_pwrite_result_encode(mp_writer *w, const uni_syscall_fs_pwrite_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_PWRITE);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_u64(w, 0u);
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_pwrite` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_fs_pwrite_result_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_pwrite_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_PWRITE) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            (void)mpr_u64(r);
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct {
    uint32_t fd;
    int64_t offset;
    uint32_t whence;
} uni_syscall_fs_lseek_request;

/* Encode one `fs_lseek` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_fs_lseek_request_encode(mp_writer *w, uint32_t fd, int64_t offset, uint32_t whence) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_LSEEK);
    mpw_map_header(w, 3u);
    mpw_u64(w, 1u);
    mpw_u64(w, (uint64_t)(fd));
    mpw_u64(w, 2u);
    mpw_i64(w, (int64_t)(offset));
    mpw_u64(w, 3u);
    mpw_u64(w, (uint64_t)(whence));
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_lseek` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_fs_lseek_request_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_lseek_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_LSEEK) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    out->fd = (uint32_t)mpr_u64(r);
                } else if (key == 2u) {
                    out->offset = mpr_i64(r);
                } else if (key == 3u) {
                    out->whence = (uint32_t)mpr_u64(r);
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `fs_lseek`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uint64_t value;
} uni_syscall_fs_lseek_result;

/* Encode one `fs_lseek` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_fs_lseek_result_encode(mp_writer *w, const uni_syscall_fs_lseek_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_LSEEK);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_u64(w, (uint64_t)(res->value));
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_lseek` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_fs_lseek_result_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_lseek_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_LSEEK) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            out->value = mpr_u64(r);
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Encode one `fs_fstat` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_fs_fstat_request_encode(mp_writer *w, uint32_t fd) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_FSTAT);
    mpw_map_header(w, 1u);
    mpw_u64(w, 1u);
    mpw_u64(w, (uint64_t)(fd));
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_fstat` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_fs_fstat_request_decode(mp_reader *r, mp_arena *a, uint32_t *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_FSTAT) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    (*out) = (uint32_t)mpr_u64(r);
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `fs_fstat`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uni_fs_stat value;
} uni_syscall_fs_fstat_result;

/* Encode one `fs_fstat` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_fs_fstat_result_encode(mp_writer *w, const uni_syscall_fs_fstat_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_FSTAT);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        uni_fs_stat_encode(w, &(res->value));
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_fstat` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_fs_fstat_result_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_fstat_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_FSTAT) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            uni_fs_stat_decode(r, a, &(out->value));
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct {
    uni_oid oid;
    mp_str path;
} uni_syscall_fs_stat_request;

/* Encode one `fs_stat` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_fs_stat_request_encode(mp_writer *w, const uni_oid *oid, mp_str path) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_STAT);
    mpw_map_header(w, 2u);
    mpw_u64(w, 1u);
    uni_oid_encode(w, &((*oid)));
    mpw_u64(w, 2u);
    mpw_str(w, (path).data, (path).len);
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_stat` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_fs_stat_request_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_stat_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_STAT) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_oid_decode(r, a, &(out->oid));
                } else if (key == 2u) {
                    {
                        uint32_t n0 = 0;
                        const char *s0 = mpr_str(r, &n0);
                        char *p0 = (char *)mpa_alloc(a, (size_t)n0 + 1u);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            memcpy(p0, s0, n0);
                            p0[n0] = '\0';
                            (out->path).data = p0;
                            (out->path).len = n0;
                        }
                    }
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `fs_stat`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uni_fs_stat value;
} uni_syscall_fs_stat_result;

/* Encode one `fs_stat` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_fs_stat_result_encode(mp_writer *w, const uni_syscall_fs_stat_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_STAT);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        uni_fs_stat_encode(w, &(res->value));
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_stat` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_fs_stat_result_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_stat_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_STAT) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            uni_fs_stat_decode(r, a, &(out->value));
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Encode one `fs_fsync` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_fs_fsync_request_encode(mp_writer *w, uint32_t fd) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_FSYNC);
    mpw_map_header(w, 1u);
    mpw_u64(w, 1u);
    mpw_u64(w, (uint64_t)(fd));
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_fsync` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_fs_fsync_request_decode(mp_reader *r, mp_arena *a, uint32_t *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_FSYNC) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    (*out) = (uint32_t)mpr_u64(r);
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `fs_fsync`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
} uni_syscall_fs_fsync_result;

/* Encode one `fs_fsync` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_fs_fsync_result_encode(mp_writer *w, const uni_syscall_fs_fsync_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_FSYNC);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_u64(w, 0u);
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_fsync` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_fs_fsync_result_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_fsync_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_FSYNC) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            (void)mpr_u64(r);
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct { uni_fs_dirent *items; uint32_t len; } uni_syscall_fs_readdir_result_list;
typedef struct {
    uni_oid oid;
    mp_str path;
} uni_syscall_fs_readdir_request;

/* Encode one `fs_readdir` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_fs_readdir_request_encode(mp_writer *w, const uni_oid *oid, mp_str path) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_READDIR);
    mpw_map_header(w, 2u);
    mpw_u64(w, 1u);
    uni_oid_encode(w, &((*oid)));
    mpw_u64(w, 2u);
    mpw_str(w, (path).data, (path).len);
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_readdir` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_fs_readdir_request_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_readdir_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_READDIR) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_oid_decode(r, a, &(out->oid));
                } else if (key == 2u) {
                    {
                        uint32_t n0 = 0;
                        const char *s0 = mpr_str(r, &n0);
                        char *p0 = (char *)mpa_alloc(a, (size_t)n0 + 1u);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            memcpy(p0, s0, n0);
                            p0[n0] = '\0';
                            (out->path).data = p0;
                            (out->path).len = n0;
                        }
                    }
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `fs_readdir`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uni_syscall_fs_readdir_result_list value;
} uni_syscall_fs_readdir_result;

/* Encode one `fs_readdir` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_fs_readdir_result_encode(mp_writer *w, const uni_syscall_fs_readdir_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_FS_READDIR);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_array_header(w, (res->value).len);
        for (uint32_t i0 = 0; i0 < (res->value).len; i0++) {
            uni_fs_dirent_encode(w, &((res->value).items[i0]));
        }
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `fs_readdir` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_fs_readdir_result_decode(mp_reader *r, mp_arena *a, uni_syscall_fs_readdir_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_FS_READDIR) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            {
                uint32_t n0 = mpr_array_header(r);
                uni_fs_dirent *p0 = (uni_fs_dirent *)mpa_alloc(a, sizeof(uni_fs_dirent) * (size_t)n0);
                if (p0 == 0) {
                    mpr_fail(r);
                } else {
                    for (uint32_t i0 = 0; i0 < n0; i0++) {
                        uni_fs_dirent_decode(r, a, &(p0[i0]));
                    }
                    (out->value).items = p0;
                    (out->value).len = n0;
                }
            }
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct {
    uint64_t f0;
    mp_bin f1;
} uni_syscall_relation_get_key_tuple;
typedef struct { uni_syscall_relation_get_key_tuple *items; uint32_t len; } uni_syscall_relation_get_key_list;
typedef struct { uint64_t *items; uint32_t len; } uni_syscall_relation_get_select_list;
typedef struct { int has_value; mp_bin value; } uni_syscall_relation_get_result_opt2;
typedef struct { uni_syscall_relation_get_result_opt2 *items; uint32_t len; } uni_syscall_relation_get_result_list;
typedef struct { int has_value; uni_syscall_relation_get_result_list value; } uni_syscall_relation_get_result_opt;
typedef struct {
    uni_oid oid;
    mp_str table;
    uni_syscall_relation_get_key_list key;
    uni_syscall_relation_get_select_list select;
} uni_syscall_relation_get_request;

/* Encode one `relation_get` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_relation_get_request_encode(mp_writer *w, const uni_oid *oid, mp_str table, uni_syscall_relation_get_key_list key, uni_syscall_relation_get_select_list select) {
    uni_syscall_write_header(w, UNI_SYSCALL_RELATION_GET);
    mpw_map_header(w, 4u);
    mpw_u64(w, 1u);
    uni_oid_encode(w, &((*oid)));
    mpw_u64(w, 2u);
    mpw_str(w, (table).data, (table).len);
    mpw_u64(w, 3u);
    mpw_array_header(w, (key).len);
    for (uint32_t i0 = 0; i0 < (key).len; i0++) {
        mpw_array_header(w, 2u);
        mpw_u64(w, (uint64_t)(((key).items[i0]).f0));
        mpw_bin(w, (((key).items[i0]).f1).data, (((key).items[i0]).f1).len);
    }
    mpw_u64(w, 4u);
    mpw_array_header(w, (select).len);
    for (uint32_t i0 = 0; i0 < (select).len; i0++) {
        mpw_u64(w, (uint64_t)((select).items[i0]));
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `relation_get` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_relation_get_request_decode(mp_reader *r, mp_arena *a, uni_syscall_relation_get_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_RELATION_GET) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_oid_decode(r, a, &(out->oid));
                } else if (key == 2u) {
                    {
                        uint32_t n0 = 0;
                        const char *s0 = mpr_str(r, &n0);
                        char *p0 = (char *)mpa_alloc(a, (size_t)n0 + 1u);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            memcpy(p0, s0, n0);
                            p0[n0] = '\0';
                            (out->table).data = p0;
                            (out->table).len = n0;
                        }
                    }
                } else if (key == 3u) {
                    {
                        uint32_t n0 = mpr_array_header(r);
                        uni_syscall_relation_get_key_tuple *p0 = (uni_syscall_relation_get_key_tuple *)mpa_alloc(a, sizeof(uni_syscall_relation_get_key_tuple) * (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            for (uint32_t i0 = 0; i0 < n0; i0++) {
                                memset(&(p0[i0]), 0, sizeof(p0[i0]));
                                if (mpr_array_header(r) != 2u) {
                                    mpr_fail(r);
                                } else {
                                    (p0[i0]).f0 = mpr_u64(r);
                                    {
                                        uint32_t n2 = 0;
                                        const uint8_t *s2 = mpr_bin(r, &n2);
                                        uint8_t *p2 = (uint8_t *)mpa_alloc(a, (size_t)n2);
                                        if (p2 == 0) {
                                            mpr_fail(r);
                                        } else {
                                            memcpy(p2, s2, n2);
                                            ((p0[i0]).f1).data = p2;
                                            ((p0[i0]).f1).len = n2;
                                        }
                                    }
                                }
                            }
                            (out->key).items = p0;
                            (out->key).len = n0;
                        }
                    }
                } else if (key == 4u) {
                    {
                        uint32_t n0 = mpr_array_header(r);
                        uint64_t *p0 = (uint64_t *)mpa_alloc(a, sizeof(uint64_t) * (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            for (uint32_t i0 = 0; i0 < n0; i0++) {
                                p0[i0] = mpr_u64(r);
                            }
                            (out->select).items = p0;
                            (out->select).len = n0;
                        }
                    }
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `relation_get`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uni_syscall_relation_get_result_opt value;
} uni_syscall_relation_get_result;

/* Encode one `relation_get` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_relation_get_result_encode(mp_writer *w, const uni_syscall_relation_get_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_RELATION_GET);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        if ((res->value).has_value) {
            mpw_array_header(w, ((res->value).value).len);
            for (uint32_t i1 = 0; i1 < ((res->value).value).len; i1++) {
                if ((((res->value).value).items[i1]).has_value) {
                    mpw_bin(w, ((((res->value).value).items[i1]).value).data, ((((res->value).value).items[i1]).value).len);
                } else {
                    mpw_nil(w);
                }
            }
        } else {
            mpw_nil(w);
        }
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `relation_get` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_relation_get_result_decode(mp_reader *r, mp_arena *a, uni_syscall_relation_get_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_RELATION_GET) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            memset(&(out->value), 0, sizeof(out->value));
            if (mpr_try_nil(r)) {
                (out->value).has_value = 0;
            } else {
                (out->value).has_value = 1;
                {
                    uint32_t n1 = mpr_array_header(r);
                    uni_syscall_relation_get_result_opt2 *p1 = (uni_syscall_relation_get_result_opt2 *)mpa_alloc(a, sizeof(uni_syscall_relation_get_result_opt2) * (size_t)n1);
                    if (p1 == 0) {
                        mpr_fail(r);
                    } else {
                        for (uint32_t i1 = 0; i1 < n1; i1++) {
                            memset(&(p1[i1]), 0, sizeof(p1[i1]));
                            if (mpr_try_nil(r)) {
                                (p1[i1]).has_value = 0;
                            } else {
                                (p1[i1]).has_value = 1;
                                {
                                    uint32_t n3 = 0;
                                    const uint8_t *s3 = mpr_bin(r, &n3);
                                    uint8_t *p3 = (uint8_t *)mpa_alloc(a, (size_t)n3);
                                    if (p3 == 0) {
                                        mpr_fail(r);
                                    } else {
                                        memcpy(p3, s3, n3);
                                        ((p1[i1]).value).data = p3;
                                        ((p1[i1]).value).len = n3;
                                    }
                                }
                            }
                        }
                        ((out->value).value).items = p1;
                        ((out->value).value).len = n1;
                    }
                }
            }
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct {
    uint64_t f0;
    mp_bin f1;
} uni_syscall_relation_update_key_tuple;
typedef struct { uni_syscall_relation_update_key_tuple *items; uint32_t len; } uni_syscall_relation_update_key_list;
typedef struct {
    uint64_t f0;
    mp_bin f1;
} uni_syscall_relation_update_values_tuple;
typedef struct { uni_syscall_relation_update_values_tuple *items; uint32_t len; } uni_syscall_relation_update_values_list;
typedef struct {
    uint64_t f0;
    uint8_t f1;
    mp_bin f2;
} uni_syscall_relation_update_deltas_tuple;
typedef struct { uni_syscall_relation_update_deltas_tuple *items; uint32_t len; } uni_syscall_relation_update_deltas_list;
typedef struct {
    uni_oid oid;
    mp_str table;
    uni_syscall_relation_update_key_list key;
    uni_syscall_relation_update_values_list values;
    uni_syscall_relation_update_deltas_list deltas;
} uni_syscall_relation_update_request;

/* Encode one `relation_update` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_relation_update_request_encode(mp_writer *w, const uni_oid *oid, mp_str table, uni_syscall_relation_update_key_list key, uni_syscall_relation_update_values_list values, uni_syscall_relation_update_deltas_list deltas) {
    uni_syscall_write_header(w, UNI_SYSCALL_RELATION_UPDATE);
    mpw_map_header(w, 5u);
    mpw_u64(w, 1u);
    uni_oid_encode(w, &((*oid)));
    mpw_u64(w, 2u);
    mpw_str(w, (table).data, (table).len);
    mpw_u64(w, 3u);
    mpw_array_header(w, (key).len);
    for (uint32_t i0 = 0; i0 < (key).len; i0++) {
        mpw_array_header(w, 2u);
        mpw_u64(w, (uint64_t)(((key).items[i0]).f0));
        mpw_bin(w, (((key).items[i0]).f1).data, (((key).items[i0]).f1).len);
    }
    mpw_u64(w, 4u);
    mpw_array_header(w, (values).len);
    for (uint32_t i0 = 0; i0 < (values).len; i0++) {
        mpw_array_header(w, 2u);
        mpw_u64(w, (uint64_t)(((values).items[i0]).f0));
        mpw_bin(w, (((values).items[i0]).f1).data, (((values).items[i0]).f1).len);
    }
    mpw_u64(w, 5u);
    mpw_array_header(w, (deltas).len);
    for (uint32_t i0 = 0; i0 < (deltas).len; i0++) {
        mpw_array_header(w, 3u);
        mpw_u64(w, (uint64_t)(((deltas).items[i0]).f0));
        mpw_u64(w, (uint64_t)(((deltas).items[i0]).f1));
        mpw_bin(w, (((deltas).items[i0]).f2).data, (((deltas).items[i0]).f2).len);
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `relation_update` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_relation_update_request_decode(mp_reader *r, mp_arena *a, uni_syscall_relation_update_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_RELATION_UPDATE) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_oid_decode(r, a, &(out->oid));
                } else if (key == 2u) {
                    {
                        uint32_t n0 = 0;
                        const char *s0 = mpr_str(r, &n0);
                        char *p0 = (char *)mpa_alloc(a, (size_t)n0 + 1u);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            memcpy(p0, s0, n0);
                            p0[n0] = '\0';
                            (out->table).data = p0;
                            (out->table).len = n0;
                        }
                    }
                } else if (key == 3u) {
                    {
                        uint32_t n0 = mpr_array_header(r);
                        uni_syscall_relation_update_key_tuple *p0 = (uni_syscall_relation_update_key_tuple *)mpa_alloc(a, sizeof(uni_syscall_relation_update_key_tuple) * (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            for (uint32_t i0 = 0; i0 < n0; i0++) {
                                memset(&(p0[i0]), 0, sizeof(p0[i0]));
                                if (mpr_array_header(r) != 2u) {
                                    mpr_fail(r);
                                } else {
                                    (p0[i0]).f0 = mpr_u64(r);
                                    {
                                        uint32_t n2 = 0;
                                        const uint8_t *s2 = mpr_bin(r, &n2);
                                        uint8_t *p2 = (uint8_t *)mpa_alloc(a, (size_t)n2);
                                        if (p2 == 0) {
                                            mpr_fail(r);
                                        } else {
                                            memcpy(p2, s2, n2);
                                            ((p0[i0]).f1).data = p2;
                                            ((p0[i0]).f1).len = n2;
                                        }
                                    }
                                }
                            }
                            (out->key).items = p0;
                            (out->key).len = n0;
                        }
                    }
                } else if (key == 4u) {
                    {
                        uint32_t n0 = mpr_array_header(r);
                        uni_syscall_relation_update_values_tuple *p0 = (uni_syscall_relation_update_values_tuple *)mpa_alloc(a, sizeof(uni_syscall_relation_update_values_tuple) * (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            for (uint32_t i0 = 0; i0 < n0; i0++) {
                                memset(&(p0[i0]), 0, sizeof(p0[i0]));
                                if (mpr_array_header(r) != 2u) {
                                    mpr_fail(r);
                                } else {
                                    (p0[i0]).f0 = mpr_u64(r);
                                    {
                                        uint32_t n2 = 0;
                                        const uint8_t *s2 = mpr_bin(r, &n2);
                                        uint8_t *p2 = (uint8_t *)mpa_alloc(a, (size_t)n2);
                                        if (p2 == 0) {
                                            mpr_fail(r);
                                        } else {
                                            memcpy(p2, s2, n2);
                                            ((p0[i0]).f1).data = p2;
                                            ((p0[i0]).f1).len = n2;
                                        }
                                    }
                                }
                            }
                            (out->values).items = p0;
                            (out->values).len = n0;
                        }
                    }
                } else if (key == 5u) {
                    {
                        uint32_t n0 = mpr_array_header(r);
                        uni_syscall_relation_update_deltas_tuple *p0 = (uni_syscall_relation_update_deltas_tuple *)mpa_alloc(a, sizeof(uni_syscall_relation_update_deltas_tuple) * (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            for (uint32_t i0 = 0; i0 < n0; i0++) {
                                memset(&(p0[i0]), 0, sizeof(p0[i0]));
                                if (mpr_array_header(r) != 3u) {
                                    mpr_fail(r);
                                } else {
                                    (p0[i0]).f0 = mpr_u64(r);
                                    (p0[i0]).f1 = (uint8_t)mpr_u64(r);
                                    {
                                        uint32_t n2 = 0;
                                        const uint8_t *s2 = mpr_bin(r, &n2);
                                        uint8_t *p2 = (uint8_t *)mpa_alloc(a, (size_t)n2);
                                        if (p2 == 0) {
                                            mpr_fail(r);
                                        } else {
                                            memcpy(p2, s2, n2);
                                            ((p0[i0]).f2).data = p2;
                                            ((p0[i0]).f2).len = n2;
                                        }
                                    }
                                }
                            }
                            (out->deltas).items = p0;
                            (out->deltas).len = n0;
                        }
                    }
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `relation_update`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
    uint64_t value;
} uni_syscall_relation_update_result;

/* Encode one `relation_update` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_relation_update_result_encode(mp_writer *w, const uni_syscall_relation_update_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_RELATION_UPDATE);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_u64(w, (uint64_t)(res->value));
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `relation_update` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_relation_update_result_decode(mp_reader *r, mp_arena *a, uni_syscall_relation_update_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_RELATION_UPDATE) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            out->value = mpr_u64(r);
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

typedef struct {
    uint64_t f0;
    mp_bin f1;
} uni_syscall_relation_insert_key_tuple;
typedef struct { uni_syscall_relation_insert_key_tuple *items; uint32_t len; } uni_syscall_relation_insert_key_list;
typedef struct {
    uint64_t f0;
    mp_bin f1;
} uni_syscall_relation_insert_values_tuple;
typedef struct { uni_syscall_relation_insert_values_tuple *items; uint32_t len; } uni_syscall_relation_insert_values_list;
typedef struct {
    uni_oid oid;
    mp_str table;
    uni_syscall_relation_insert_key_list key;
    uni_syscall_relation_insert_values_list values;
} uni_syscall_relation_insert_request;

/* Encode one `relation_insert` request frame (header + map body of the
 * WIT-declared arguments). Encode only borrows the argument values. */
MP_INLINE int uni_syscall_relation_insert_request_encode(mp_writer *w, const uni_oid *oid, mp_str table, uni_syscall_relation_insert_key_list key, uni_syscall_relation_insert_values_list values) {
    uni_syscall_write_header(w, UNI_SYSCALL_RELATION_INSERT);
    mpw_map_header(w, 4u);
    mpw_u64(w, 1u);
    uni_oid_encode(w, &((*oid)));
    mpw_u64(w, 2u);
    mpw_str(w, (table).data, (table).len);
    mpw_u64(w, 3u);
    mpw_array_header(w, (key).len);
    for (uint32_t i0 = 0; i0 < (key).len; i0++) {
        mpw_array_header(w, 2u);
        mpw_u64(w, (uint64_t)(((key).items[i0]).f0));
        mpw_bin(w, (((key).items[i0]).f1).data, (((key).items[i0]).f1).len);
    }
    mpw_u64(w, 4u);
    mpw_array_header(w, (values).len);
    for (uint32_t i0 = 0; i0 < (values).len; i0++) {
        mpw_array_header(w, 2u);
        mpw_u64(w, (uint64_t)(((values).items[i0]).f0));
        mpw_bin(w, (((values).items[i0]).f1).data, (((values).items[i0]).f1).len);
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `relation_insert` request frame into `out` (arena contract
 * of the generated decoders). Lenient: unknown keys skipped, missing
 * parameters defaulted. */
MP_INLINE int uni_syscall_relation_insert_request_decode(mp_reader *r, mp_arena *a, uni_syscall_relation_insert_request *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_RELATION_INSERT) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    uni_oid_decode(r, a, &(out->oid));
                } else if (key == 2u) {
                    {
                        uint32_t n0 = 0;
                        const char *s0 = mpr_str(r, &n0);
                        char *p0 = (char *)mpa_alloc(a, (size_t)n0 + 1u);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            memcpy(p0, s0, n0);
                            p0[n0] = '\0';
                            (out->table).data = p0;
                            (out->table).len = n0;
                        }
                    }
                } else if (key == 3u) {
                    {
                        uint32_t n0 = mpr_array_header(r);
                        uni_syscall_relation_insert_key_tuple *p0 = (uni_syscall_relation_insert_key_tuple *)mpa_alloc(a, sizeof(uni_syscall_relation_insert_key_tuple) * (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            for (uint32_t i0 = 0; i0 < n0; i0++) {
                                memset(&(p0[i0]), 0, sizeof(p0[i0]));
                                if (mpr_array_header(r) != 2u) {
                                    mpr_fail(r);
                                } else {
                                    (p0[i0]).f0 = mpr_u64(r);
                                    {
                                        uint32_t n2 = 0;
                                        const uint8_t *s2 = mpr_bin(r, &n2);
                                        uint8_t *p2 = (uint8_t *)mpa_alloc(a, (size_t)n2);
                                        if (p2 == 0) {
                                            mpr_fail(r);
                                        } else {
                                            memcpy(p2, s2, n2);
                                            ((p0[i0]).f1).data = p2;
                                            ((p0[i0]).f1).len = n2;
                                        }
                                    }
                                }
                            }
                            (out->key).items = p0;
                            (out->key).len = n0;
                        }
                    }
                } else if (key == 4u) {
                    {
                        uint32_t n0 = mpr_array_header(r);
                        uni_syscall_relation_insert_values_tuple *p0 = (uni_syscall_relation_insert_values_tuple *)mpa_alloc(a, sizeof(uni_syscall_relation_insert_values_tuple) * (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            for (uint32_t i0 = 0; i0 < n0; i0++) {
                                memset(&(p0[i0]), 0, sizeof(p0[i0]));
                                if (mpr_array_header(r) != 2u) {
                                    mpr_fail(r);
                                } else {
                                    (p0[i0]).f0 = mpr_u64(r);
                                    {
                                        uint32_t n2 = 0;
                                        const uint8_t *s2 = mpr_bin(r, &n2);
                                        uint8_t *p2 = (uint8_t *)mpa_alloc(a, (size_t)n2);
                                        if (p2 == 0) {
                                            mpr_fail(r);
                                        } else {
                                            memcpy(p2, s2, n2);
                                            ((p0[i0]).f1).data = p2;
                                            ((p0[i0]).f1).len = n2;
                                        }
                                    }
                                }
                            }
                            (out->values).items = p0;
                            (out->values).len = n0;
                        }
                    }
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Wire-level `result<T, E>` of `relation_insert`: a success carries
 * `value` (encoded as `[0, value]`), a failure carries `err` (encoded as
 * `[1, err]`). Protocol-level decode failures return -1 instead. */
typedef struct {
    int is_err;
    uni_error err;
} uni_syscall_relation_insert_result;

/* Encode one `relation_insert` result frame (header + `[tag, payload]`
 * body). */
MP_INLINE int uni_syscall_relation_insert_result_encode(mp_writer *w, const uni_syscall_relation_insert_result *res) {
    uni_syscall_write_header(w, UNI_SYSCALL_RELATION_INSERT);
    mpw_array_header(w, 2u);
    if (res->is_err) {
        mpw_u64(w, 1u);
        uni_error_encode(w, &(res->err));
    } else {
        mpw_u64(w, 0u);
        mpw_u64(w, 0u);
    }
    return mpw_ok(w) ? 0 : -1;
}

/* Decode one `relation_insert` result frame into `out` (arena contract
 * of the generated decoders). */
MP_INLINE int uni_syscall_relation_insert_result_decode(mp_reader *r, mp_arena *a, uni_syscall_relation_insert_result *out) {
    uni_syscall_message_kind kind;
    (void)a;
    if (uni_syscall_decode_header(r, &kind) != 0) {
        return -1;
    }
    if (kind != UNI_SYSCALL_RELATION_INSERT) {
        mpr_fail(r);
        return -1;
    }
    memset(out, 0, sizeof(*out));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        if (tag == 0u) {
            out->is_err = 0;
            (void)mpr_u64(r);
        } else if (tag == 1u) {
            out->is_err = 1;
            uni_error_decode(r, a, &(out->err));
        } else {
            mpr_fail(r);
            return -1;
        }
    }
    if (!mpr_done(r)) {
        mpr_fail(r);
        return -1;
    }
    return mpr_ok(r) ? 0 : -1;
}

#ifdef __cplusplus
}
#endif

#endif /* MUDUD_TYPES_UNI_SYSCALL_H */