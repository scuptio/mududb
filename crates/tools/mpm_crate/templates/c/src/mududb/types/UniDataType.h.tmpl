// Generated by mudu_gen. Do not edit manually.
/*
 * mududb C binding message types/codecs (mgen message -l c
 * --with-func-codec). Self-contained header: `MP_INLINE` functions only
 * (`static inline` in C, `inline` in C++), no mutable state. Ownership
 * contract: encode borrows its inputs; decode copies every variable-length
 * value into the caller's `mp_arena`; the writer owns its buffer
 * (`mpw_free`). See the package readme.md.
 */
#ifndef MUDUD_TYPES_UNI_DATA_TYPE_H
#define MUDUD_TYPES_UNI_DATA_TYPE_H

#include <stdbool.h>
#include <stdint.h>
#include <string.h>

#include "mududb/codec/mpack.h"
#include "mududb/types/UniScalar.h"

#ifdef __cplusplus
extern "C" {
#endif

#ifndef MUDUD_FWD_UNI_DATA_TYPE
#define MUDUD_FWD_UNI_DATA_TYPE
typedef struct uni_data_type uni_data_type;
MP_INLINE void uni_data_type_encode(mp_writer *w, const uni_data_type *value);
MP_INLINE int uni_data_type_decode(mp_reader *r, mp_arena *a, uni_data_type *out);
MP_INLINE uni_data_type *uni_data_type_decode_new(mp_reader *r, mp_arena *a);
#endif /* MUDUD_FWD_UNI_DATA_TYPE */
#ifndef MUDUD_FWD_UNI_RECORD_TYPE
#define MUDUD_FWD_UNI_RECORD_TYPE
typedef struct uni_record_type uni_record_type;
MP_INLINE void uni_record_type_encode(mp_writer *w, const uni_record_type *value);
MP_INLINE int uni_record_type_decode(mp_reader *r, mp_arena *a, uni_record_type *out);
MP_INLINE uni_record_type *uni_record_type_decode_new(mp_reader *r, mp_arena *a);
#endif /* MUDUD_FWD_UNI_RECORD_TYPE */
#ifndef MUDUD_FWD_UNI_RESULT_TYPE
#define MUDUD_FWD_UNI_RESULT_TYPE
typedef struct uni_result_type uni_result_type;
MP_INLINE void uni_result_type_encode(mp_writer *w, const uni_result_type *value);
MP_INLINE int uni_result_type_decode(mp_reader *r, mp_arena *a, uni_result_type *out);
MP_INLINE uni_result_type *uni_result_type_decode_new(mp_reader *r, mp_arena *a);
#endif /* MUDUD_FWD_UNI_RESULT_TYPE */

typedef enum {
    UNI_DATA_TYPE_SCALAR = 0u,
    UNI_DATA_TYPE_ARRAY = 1u,
    UNI_DATA_TYPE_RECORD = 2u,
    UNI_DATA_TYPE_OPTION = 3u,
    UNI_DATA_TYPE_TUPLE = 4u,
    UNI_DATA_TYPE_RESULT = 5u,
    UNI_DATA_TYPE_IDENTIFIER = 6u,
    UNI_DATA_TYPE_BINARY = 7u,
    UNI_DATA_TYPE_BOX = 8u,
} uni_data_type_kind;

typedef struct { uni_data_type *items; uint32_t len; } uni_data_type_tuple_list;
struct uni_data_type {
    uni_data_type_kind kind;
    union {
        uni_scalar scalar;
        uni_data_type * array;
        uni_record_type * record;
        uni_data_type * option;
        uni_data_type_tuple_list tuple;
        uni_result_type * result;
        mp_str identifier;
        uni_data_type * box;
    } as;
};

/* Encode `value` as a `[tag, payload]` 2-array; payload-less cases carry a
 * `0u8` placeholder (NOT nil), matching the Rust host's variant serde. An
 * unknown kind flags `w` (check `mpw_ok`) instead of emitting a corrupt
 * value. */
MP_INLINE void uni_data_type_encode(mp_writer *w, const uni_data_type *value) {
    mpw_array_header(w, 2u);
    mpw_u64(w, (uint64_t)value->kind);
    switch (value->kind) {
    case UNI_DATA_TYPE_SCALAR:
        uni_scalar_encode(w, &(value->as.scalar));
        break;
    case UNI_DATA_TYPE_ARRAY:
        if ((value->as.array) == 0) {
            mpw_fail(w);
        } else {
            uni_data_type_encode(w, &((*value->as.array)));
        }
        break;
    case UNI_DATA_TYPE_RECORD:
        if ((value->as.record) == 0) {
            mpw_fail(w);
        } else {
            uni_record_type_encode(w, &((*value->as.record)));
        }
        break;
    case UNI_DATA_TYPE_OPTION:
        if ((value->as.option) == 0) {
            mpw_fail(w);
        } else {
            uni_data_type_encode(w, &((*value->as.option)));
        }
        break;
    case UNI_DATA_TYPE_TUPLE:
        mpw_array_header(w, (value->as.tuple).len);
        for (uint32_t i0 = 0; i0 < (value->as.tuple).len; i0++) {
            uni_data_type_encode(w, &((value->as.tuple).items[i0]));
        }
        break;
    case UNI_DATA_TYPE_RESULT:
        if ((value->as.result) == 0) {
            mpw_fail(w);
        } else {
            uni_result_type_encode(w, &((*value->as.result)));
        }
        break;
    case UNI_DATA_TYPE_IDENTIFIER:
        mpw_str(w, (value->as.identifier).data, (value->as.identifier).len);
        break;
    case UNI_DATA_TYPE_BINARY:
        /* payload-less case: the `0u8` placeholder (NOT nil), matching
         * the Rust host's variant serde. */
        mpw_u64(w, 0u);
        break;
    case UNI_DATA_TYPE_BOX:
        if ((value->as.box) == 0) {
            mpw_fail(w);
        } else {
            uni_data_type_encode(w, &((*value->as.box)));
        }
        break;
    default:
        /* unknown kind: flag the writer instead of emitting a corrupt value */
        mpw_fail(w);
        break;
    }
}

/* Decode one variant into `value` (arena contract of the generated
 * decoders). 0 on success, -1 (sticky error set) on a malformed shape or an
 * unknown tag. */
MP_INLINE int uni_data_type_decode(mp_reader *r, mp_arena *a, uni_data_type *value) {
    (void)a;
    memset(value, 0, sizeof(*value));
    if (mpr_array_header(r) != 2u) {
        mpr_fail(r);
        return -1;
    }
    {
        uint64_t tag = mpr_u64(r);
        switch (tag) {
        case 0u:
            value->kind = UNI_DATA_TYPE_SCALAR;
            uni_scalar_decode(r, a, &(value->as.scalar));
            break;
        case 1u:
            value->kind = UNI_DATA_TYPE_ARRAY;
            {
                uni_data_type *p0 = uni_data_type_decode_new(r, a);
                if (p0 == 0) {
                    mpr_fail(r);
                } else {
                    value->as.array = p0;
                }
            }
            break;
        case 2u:
            value->kind = UNI_DATA_TYPE_RECORD;
            {
                uni_record_type *p0 = uni_record_type_decode_new(r, a);
                if (p0 == 0) {
                    mpr_fail(r);
                } else {
                    value->as.record = p0;
                }
            }
            break;
        case 3u:
            value->kind = UNI_DATA_TYPE_OPTION;
            {
                uni_data_type *p0 = uni_data_type_decode_new(r, a);
                if (p0 == 0) {
                    mpr_fail(r);
                } else {
                    value->as.option = p0;
                }
            }
            break;
        case 4u:
            value->kind = UNI_DATA_TYPE_TUPLE;
            {
                uint32_t n0 = mpr_array_header(r);
                uni_data_type *p0 = (uni_data_type *)mpa_alloc(a, sizeof(uni_data_type) * (size_t)n0);
                if (p0 == 0) {
                    mpr_fail(r);
                } else {
                    for (uint32_t i0 = 0; i0 < n0; i0++) {
                        uni_data_type_decode(r, a, &(p0[i0]));
                    }
                    (value->as.tuple).items = p0;
                    (value->as.tuple).len = n0;
                }
            }
            break;
        case 5u:
            value->kind = UNI_DATA_TYPE_RESULT;
            {
                uni_result_type *p0 = uni_result_type_decode_new(r, a);
                if (p0 == 0) {
                    mpr_fail(r);
                } else {
                    value->as.result = p0;
                }
            }
            break;
        case 6u:
            value->kind = UNI_DATA_TYPE_IDENTIFIER;
            {
                uint32_t n0 = 0;
                const char *s0 = mpr_str(r, &n0);
                char *p0 = (char *)mpa_alloc(a, (size_t)n0 + 1u);
                if (p0 == 0) {
                    mpr_fail(r);
                } else {
                    memcpy(p0, s0, n0);
                    p0[n0] = '\0';
                    (value->as.identifier).data = p0;
                    (value->as.identifier).len = n0;
                }
            }
            break;
        case 7u:
            value->kind = UNI_DATA_TYPE_BINARY;
            /* consume the `0u8` placeholder */
            (void)mpr_u64(r);
            break;
        case 8u:
            value->kind = UNI_DATA_TYPE_BOX;
            {
                uni_data_type *p0 = uni_data_type_decode_new(r, a);
                if (p0 == 0) {
                    mpr_fail(r);
                } else {
                    value->as.box = p0;
                }
            }
            break;
        default:
            mpr_fail(r);
            return -1;
        }
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Decode one variant into freshly arena-allocated storage; 0 on failure
 * (the reader's sticky error is set). Boxes of this type decode through
 * this function, so headers referencing it by pointer never need its
 * `sizeof`. */
MP_INLINE uni_data_type *uni_data_type_decode_new(mp_reader *r, mp_arena *a) {
    uni_data_type *p = (uni_data_type *)mpa_alloc(a, sizeof(uni_data_type));
    if (p == 0) {
        mpr_fail(r);
        return 0;
    }
    if (uni_data_type_decode(r, a, p) != 0) {
        return 0;
    }
    return p;
}

#ifdef __cplusplus
}
#endif
#include "mududb/types/UniRecordType.h"
#include "mududb/types/UniResultType.h"

#endif /* MUDUD_TYPES_UNI_DATA_TYPE_H */