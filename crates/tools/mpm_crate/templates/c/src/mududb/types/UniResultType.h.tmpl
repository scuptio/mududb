// Generated by mudu_gen. Do not edit manually.
/*
 * mududb C binding message types/codecs (mgen message -l c
 * --with-func-codec). Self-contained header: `MP_INLINE` functions only
 * (`static inline` in C, `inline` in C++), no mutable state. Ownership
 * contract: encode borrows its inputs; decode copies every variable-length
 * value into the caller's `mp_arena`; the writer owns its buffer
 * (`mpw_free`). See the package readme.md.
 */
#ifndef MUDUD_TYPES_UNI_RESULT_TYPE_H
#define MUDUD_TYPES_UNI_RESULT_TYPE_H

#include <stdbool.h>
#include <stdint.h>
#include <string.h>

#include "mududb/codec/mpack.h"

#ifdef __cplusplus
extern "C" {
#endif

#ifndef MUDUD_FWD_UNI_RESULT_TYPE
#define MUDUD_FWD_UNI_RESULT_TYPE
typedef struct uni_result_type uni_result_type;
MP_INLINE void uni_result_type_encode(mp_writer *w, const uni_result_type *value);
MP_INLINE int uni_result_type_decode(mp_reader *r, mp_arena *a, uni_result_type *out);
MP_INLINE uni_result_type *uni_result_type_decode_new(mp_reader *r, mp_arena *a);
#endif /* MUDUD_FWD_UNI_RESULT_TYPE */
#ifndef MUDUD_FWD_UNI_DATA_TYPE
#define MUDUD_FWD_UNI_DATA_TYPE
typedef struct uni_data_type uni_data_type;
MP_INLINE void uni_data_type_encode(mp_writer *w, const uni_data_type *value);
MP_INLINE int uni_data_type_decode(mp_reader *r, mp_arena *a, uni_data_type *out);
MP_INLINE uni_data_type *uni_data_type_decode_new(mp_reader *r, mp_arena *a);
#endif /* MUDUD_FWD_UNI_DATA_TYPE */

struct uni_result_type {
    uni_data_type * ok;
    bool ok_is_null;
    uni_data_type * err;
    bool err_is_null;
};

/* Encode `value` as a MessagePack map keyed by the 1-based field numbers
 * (WIT declaration order); `option` fields are omitted when null (proto3
 * presence semantics). Encode only borrows `value`; failures are sticky in
 * `w` (check `mpw_ok`). */
MP_INLINE void uni_result_type_encode(mp_writer *w, const uni_result_type *value) {
    mpw_map_header(w, 2u - 1u + (value->ok_is_null ? 0u : 1u) - 1u + (value->err_is_null ? 0u : 1u));
    if (!value->ok_is_null) {
        mpw_u64(w, 1u);
        if ((value->ok) == 0) {
            mpw_fail(w);
        } else {
            uni_data_type_encode(w, &((*value->ok)));
        }
    }
    if (!value->err_is_null) {
        mpw_u64(w, 2u);
        if ((value->err) == 0) {
            mpw_fail(w);
        } else {
            uni_data_type_encode(w, &((*value->err)));
        }
    }
}

/* Decode one record into `out`, borrowing nothing from the input: every
 * string, blob, array and boxed value is copied into `a`. Lenient: unknown
 * keys are skipped and missing fields keep their zero default (absent
 * `option` fields decode as null). 0 on success, -1 (with the reader's
 * sticky error set) on malformed input or arena exhaustion. */
MP_INLINE int uni_result_type_decode(mp_reader *r, mp_arena *a, uni_result_type *out) {
    (void)a;
    memset(out, 0, sizeof(*out));
    out->ok_is_null = 1;
    out->err_is_null = 1;
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    if (mpr_try_nil(r)) {
                        out->ok_is_null = 1;
                    } else {
                        out->ok_is_null = 0;
                        {
                            uni_data_type *p0 = uni_data_type_decode_new(r, a);
                            if (p0 == 0) {
                                mpr_fail(r);
                            } else {
                                out->ok = p0;
                            }
                        }
                    }
                } else if (key == 2u) {
                    if (mpr_try_nil(r)) {
                        out->err_is_null = 1;
                    } else {
                        out->err_is_null = 0;
                        {
                            uni_data_type *p0 = uni_data_type_decode_new(r, a);
                            if (p0 == 0) {
                                mpr_fail(r);
                            } else {
                                out->err = p0;
                            }
                        }
                    }
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Decode one record into freshly arena-allocated storage; 0 on failure
 * (the reader's sticky error is set). Boxes of this type decode through
 * this function, so headers referencing it by pointer never need its
 * `sizeof`. */
MP_INLINE uni_result_type *uni_result_type_decode_new(mp_reader *r, mp_arena *a) {
    uni_result_type *p = (uni_result_type *)mpa_alloc(a, sizeof(uni_result_type));
    if (p == 0) {
        mpr_fail(r);
        return 0;
    }
    if (uni_result_type_decode(r, a, p) != 0) {
        return 0;
    }
    return p;
}

#ifdef __cplusplus
}
#endif
#include "mududb/types/UniDataType.h"

#endif /* MUDUD_TYPES_UNI_RESULT_TYPE_H */