// Generated by mudu_gen. Do not edit manually.
/*
 * mududb C binding message types/codecs (mgen message -l c
 * --with-func-codec). Self-contained header: `MP_INLINE` functions only
 * (`static inline` in C, `inline` in C++), no mutable state. Ownership
 * contract: encode borrows its inputs; decode copies every variable-length
 * value into the caller's `mp_arena`; the writer owns its buffer
 * (`mpw_free`). See the package readme.md.
 */
#ifndef MUDUD_TYPES_UNI_SQL_PARAM_H
#define MUDUD_TYPES_UNI_SQL_PARAM_H

#include <stdbool.h>
#include <stdint.h>
#include <string.h>

#include "mududb/codec/mpack.h"
#include "mududb/types/UniDataValue.h"

#ifdef __cplusplus
extern "C" {
#endif

#ifndef MUDUD_FWD_UNI_SQL_PARAM
#define MUDUD_FWD_UNI_SQL_PARAM
typedef struct uni_sql_param uni_sql_param;
MP_INLINE void uni_sql_param_encode(mp_writer *w, const uni_sql_param *value);
MP_INLINE int uni_sql_param_decode(mp_reader *r, mp_arena *a, uni_sql_param *out);
MP_INLINE uni_sql_param *uni_sql_param_decode_new(mp_reader *r, mp_arena *a);
#endif /* MUDUD_FWD_UNI_SQL_PARAM */

typedef struct { uni_data_value *items; uint32_t len; } uni_sql_param_params_list;
typedef struct { mp_str *items; uint32_t len; } uni_sql_param_param_names_list;
struct uni_sql_param {
    uni_sql_param_params_list params;
    uni_sql_param_param_names_list param_names;
    bool param_names_is_null;
};

/* Encode `value` as a MessagePack map keyed by the 1-based field numbers
 * (WIT declaration order); `option` fields are omitted when null (proto3
 * presence semantics). Encode only borrows `value`; failures are sticky in
 * `w` (check `mpw_ok`). */
MP_INLINE void uni_sql_param_encode(mp_writer *w, const uni_sql_param *value) {
    mpw_map_header(w, 2u - 1u + (value->param_names_is_null ? 0u : 1u));
    mpw_u64(w, 1u);
    mpw_array_header(w, (value->params).len);
    for (uint32_t i0 = 0; i0 < (value->params).len; i0++) {
        uni_data_value_encode(w, &((value->params).items[i0]));
    }
    if (!value->param_names_is_null) {
        mpw_u64(w, 2u);
        mpw_array_header(w, (value->param_names).len);
        for (uint32_t i0 = 0; i0 < (value->param_names).len; i0++) {
            mpw_str(w, ((value->param_names).items[i0]).data, ((value->param_names).items[i0]).len);
        }
    }
}

/* Decode one record into `out`, borrowing nothing from the input: every
 * string, blob, array and boxed value is copied into `a`. Lenient: unknown
 * keys are skipped and missing fields keep their zero default (absent
 * `option` fields decode as null). 0 on success, -1 (with the reader's
 * sticky error set) on malformed input or arena exhaustion. */
MP_INLINE int uni_sql_param_decode(mp_reader *r, mp_arena *a, uni_sql_param *out) {
    (void)a;
    memset(out, 0, sizeof(*out));
    out->param_names_is_null = 1;
    {
        uint32_t count = mpr_map_header(r);
        for (uint32_t i = 0; i < count && mpr_ok(r); i++) {
            if (mpr_next_is_integer(r)) {
                uint64_t key = mpr_u64(r);
                if (key == 1u) {
                    {
                        uint32_t n0 = mpr_array_header(r);
                        uni_data_value *p0 = (uni_data_value *)mpa_alloc(a, sizeof(uni_data_value) * (size_t)n0);
                        if (p0 == 0) {
                            mpr_fail(r);
                        } else {
                            for (uint32_t i0 = 0; i0 < n0; i0++) {
                                uni_data_value_decode(r, a, &(p0[i0]));
                            }
                            (out->params).items = p0;
                            (out->params).len = n0;
                        }
                    }
                } else if (key == 2u) {
                    if (mpr_try_nil(r)) {
                        out->param_names_is_null = 1;
                    } else {
                        out->param_names_is_null = 0;
                        {
                            uint32_t n0 = mpr_array_header(r);
                            mp_str *p0 = (mp_str *)mpa_alloc(a, sizeof(mp_str) * (size_t)n0);
                            if (p0 == 0) {
                                mpr_fail(r);
                            } else {
                                for (uint32_t i0 = 0; i0 < n0; i0++) {
                                    {
                                        uint32_t n1 = 0;
                                        const char *s1 = mpr_str(r, &n1);
                                        char *p1 = (char *)mpa_alloc(a, (size_t)n1 + 1u);
                                        if (p1 == 0) {
                                            mpr_fail(r);
                                        } else {
                                            memcpy(p1, s1, n1);
                                            p1[n1] = '\0';
                                            (p0[i0]).data = p1;
                                            (p0[i0]).len = n1;
                                        }
                                    }
                                }
                                (out->param_names).items = p0;
                                (out->param_names).len = n0;
                            }
                        }
                    }
                } else {
                    mpr_skip(r);
                }
            } else {
                mpr_skip(r);
                mpr_skip(r);
            }
        }
    }
    return mpr_ok(r) ? 0 : -1;
}

/* Decode one record into freshly arena-allocated storage; 0 on failure
 * (the reader's sticky error is set). Boxes of this type decode through
 * this function, so headers referencing it by pointer never need its
 * `sizeof`. */
MP_INLINE uni_sql_param *uni_sql_param_decode_new(mp_reader *r, mp_arena *a) {
    uni_sql_param *p = (uni_sql_param *)mpa_alloc(a, sizeof(uni_sql_param));
    if (p == 0) {
        mpr_fail(r);
        return 0;
    }
    if (uni_sql_param_decode(r, a, p) != 0) {
        return 0;
    }
    return p;
}

#ifdef __cplusplus
}
#endif

#endif /* MUDUD_TYPES_UNI_SQL_PARAM_H */