# 确定性模拟与故障注入 TODO

## 目标

将 `mududb.ds` 的确定性模拟能力纳入核心回归体系，使并发与故障问题可复现。

## TODO

- [ ] 定义主仓库与 `mududb.ds` 的版本对应关系和兼容接口。
- [ ] 为调度、时间、随机数、网络、磁盘和进程故障提供统一注入点。
- [ ] 覆盖 I/O 延迟、部分写入、丢包、重复消息、乱序和 worker 重启。
- [ ] 覆盖事务交错、取消、超时、恢复和重复 replay。
- [ ] 每次模拟保存 seed、操作序列和最小失败轨迹。
- [ ] 将历史失败 seed 固化为永久回归用例。
- [ ] PR 对关键内核变更运行小规模 seed 集；定时任务扩大 seed 数和运行时长。
- [ ] 失败日志中输出可直接执行的单次复现命令。
- [ ] 明确哪些核心模块不得绕过 `mudu_sys` 直接访问非确定性来源。

## 验收标准

- [ ] 任一模拟失败均能通过 seed 在本地稳定复现。
- [ ] 内核关键提交必须通过固定回归 seed 集。
- [ ] 故障场景能够验证数据原子性、持久性和恢复后的可用性。

