# CI 质量门禁治理 TODO

## 目标

让 CI 信号稳定、可执行，避免长期存在无人处理的非阻塞失败。

## TODO

- [ ] 将检查划分为 PR 快速检查、主干完整检查和定时重型检查。
- [ ] 为 `cargo-hack` 现有 feature 组合失败建立 Issue、负责人和截止日期。
- [ ] 修复 workspace 布局导致的 `cargo-outdated` 问题，并将任务转为阻塞检查或定时告警。
- [ ] 增加 `cargo doc --workspace --no-deps` 和 rustdoc 链接检查。
- [ ] 增加 Markdown 链接、代码片段及中英文关键文档同步检查。
- [ ] 增加生成代码重新生成后的 `git diff --exit-code` 检查。
- [ ] 对 CI 中通过 `continue-on-error` 的步骤设置数量上限和到期时间。
- [ ] 使用缓存但禁止缓存掩盖缺失依赖，定期运行干净环境构建。
- [ ] 为失败任务输出简洁的复现命令和必要环境信息。

## 验收标准

- [ ] 主干不存在无负责人、无期限的持续失败任务。
- [ ] PR 必需检查全部通过后才能合并。
- [ ] 文档、生成物和 feature 组合都有自动化验证。

