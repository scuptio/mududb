# 核心模块职责拆分 TODO

## 目标

降低 worker、存储、协议和后端等核心模块的认知复杂度，同时保持行为不变。

## TODO

- [ ] 使用依赖关系、状态所有权和变更频率识别高耦合模块，不按行数机械拆分。
- [ ] 优先评审 `worker_storage.rs`、`worker.rs`、`worker_ring_loop.rs`、`protocol/mod.rs` 等大型核心文件。
- [ ] 为每个候选模块记录职责、输入输出、拥有状态和错误边界。
- [ ] 将协议解析、状态转换、I/O 驱动和资源生命周期拆成可独立测试的单元。
- [ ] 减少共享可变状态，明确 worker-local、node-local 和全局状态。
- [ ] 在重构前增加 characterization tests，锁定现有行为。
- [ ] 每次拆分保持小提交，不混合功能变化和结构变化。
- [ ] 删除重复测试基础设施，统一复用 `testing::support` 和 `test_utils`。
- [ ] 为复杂状态机增加状态图、合法转换断言和错误恢复测试。

## 验收标准

- [ ] 拆分后的模块有单一可描述职责和明确所有权。
- [ ] 核心行为、兼容性和性能基线无回归。
- [ ] 新功能不再要求跨多个无关模块同步修改相同逻辑。

