fn main() -> Result<(), Box<dyn std::error::Error>> {
    //let r_server = mududb::server::mudu_server::MuduServer::start(None);
    //let (server, _stop) = r_server.map_err(Box::new)?;
    //server.join().map_err(Box::new)?;
    Ok(())
}
