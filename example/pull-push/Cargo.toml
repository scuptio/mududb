[package]
name = "pull-push"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
mudu = { workspace = true }
sys_interface = { workspace = true, features = ["async"] }
wit-bindgen = { workspace = true }
