// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TpccSeedArgs {
    pub warehouse_id: i32,
    pub district_count: i32,
    pub customer_count: i32,
    pub item_count: i32,
    pub op_index: u64,
}

impl From<TpccSeedArgs> for super::Reducer {
    fn from(args: TpccSeedArgs) -> Self {
        Self::TpccSeed {
            warehouse_id: args.warehouse_id,
            district_count: args.district_count,
            customer_count: args.customer_count,
            item_count: args.item_count,
            op_index: args.op_index,
        }
    }
}

impl __sdk::InModule for TpccSeedArgs {
    type Module = super::RemoteModule;
}

pub struct TpccSeedCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `tpcc_seed`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait tpcc_seed {
    /// Request that the remote module invoke the reducer `tpcc_seed` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_tpcc_seed`] callbacks.
    fn tpcc_seed(
        &self,
        warehouse_id: i32,
        district_count: i32,
        customer_count: i32,
        item_count: i32,
        op_index: u64,
    ) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `tpcc_seed`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`TpccSeedCallbackId`] can be passed to [`Self::remove_on_tpcc_seed`]
    /// to cancel the callback.
    fn on_tpcc_seed(
        &self,
        callback: impl FnMut(&super::ReducerEventContext, &i32, &i32, &i32, &i32, &u64) + Send + 'static,
    ) -> TpccSeedCallbackId;
    /// Cancel a callback previously registered by [`Self::on_tpcc_seed`],
    /// causing it not to run in the future.
    fn remove_on_tpcc_seed(&self, callback: TpccSeedCallbackId);
}

impl tpcc_seed for super::RemoteReducers {
    fn tpcc_seed(
        &self,
        warehouse_id: i32,
        district_count: i32,
        customer_count: i32,
        item_count: i32,
        op_index: u64,
    ) -> __sdk::Result<()> {
        self.imp.call_reducer(
            "tpcc_seed",
            TpccSeedArgs {
                warehouse_id,
                district_count,
                customer_count,
                item_count,
                op_index,
            },
        )
    }
    fn on_tpcc_seed(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext, &i32, &i32, &i32, &i32, &u64)
        + Send
        + 'static,
    ) -> TpccSeedCallbackId {
        TpccSeedCallbackId(self.imp.on_reducer(
            "tpcc_seed",
            Box::new(move |ctx: &super::ReducerEventContext| {
                #[allow(irrefutable_let_patterns)]
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer:
                                super::Reducer::TpccSeed {
                                    warehouse_id,
                                    district_count,
                                    customer_count,
                                    item_count,
                                    op_index,
                                },
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(
                    ctx,
                    warehouse_id,
                    district_count,
                    customer_count,
                    item_count,
                    op_index,
                )
            }),
        ))
    }
    fn remove_on_tpcc_seed(&self, callback: TpccSeedCallbackId) {
        self.imp.remove_on_reducer("tpcc_seed", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `tpcc_seed`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_tpcc_seed {
    /// Set the call-reducer flags for the reducer `tpcc_seed` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn tpcc_seed(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_tpcc_seed for super::SetReducerFlags {
    fn tpcc_seed(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("tpcc_seed", flags);
    }
}
