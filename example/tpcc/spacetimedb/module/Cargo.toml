[package]
name = "tpcc-stdb-module"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = "=1.12.0"
log = "0.4"

# Standalone workspace: this crate is built for wasm32-unknown-unknown and
# must not be part of the mududb workspace.
[workspace]
