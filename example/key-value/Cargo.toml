[package]
name = "key-value"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[lints]
workspace = true

[dependencies]
mududb = { workspace = true, features = ["async"] }
wit-bindgen = { workspace = true }

[target.'cfg(not(target_arch = "wasm32"))'.dev-dependencies]
mudu_adapter = { workspace = true }
mududb = { workspace = true, features = ["async", "standalone-adapter"] }
