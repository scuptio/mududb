[package]
name = "mudu_sys"
version = "0.1.0"
edition = "2021"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(sim)'] }

[features]
debug_trace = []

[dependencies]
mudu_sys_impl = { workspace = true }
